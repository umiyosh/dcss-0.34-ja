# コードメッセージ: `death-curse.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L46 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L46](../../crawl-ref/source/death-curse.cc#L46)

### 未解決の式（断片を翻訳キーにしない）

```text
player_msg
```

## 2. L49 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L49](../../crawl-ref/source/death-curse.cc#L49)

### 未解決の式（断片を翻訳キーにしない）

```text
do_mon_str_replacements(mon_seen_msg,
            *target.as_monster(), S_SILENT)
```

## 3. L54 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L54](../../crawl-ref/source/death-curse.cc#L54)

### 未解決の式（断片を翻訳キーにしない）

```text
do_mon_str_replacements(mon_unseen_msg,
            *target.as_monster(), S_SILENT)
```

## 4. L141 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L141](../../crawl-ref/source/death-curse.cc#L141)

### 未解決の式（断片を翻訳キーにしない）

```text
*random_iterator(messages)
```

## 5. L237 — 未訳・API未接続

呼出: `simple_god_message` / [原本 L237](../../crawl-ref/source/death-curse.cc#L237)

### 原文（静的キー）

```text
 averts the curse.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 6. L242 — 未訳・API未接続

呼出: `simple_god_message` / [原本 L242](../../crawl-ref/source/death-curse.cc#L242)

### 原文（静的キー）

```text
 partially averts the curse.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。
