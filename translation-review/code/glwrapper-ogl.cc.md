# コードメッセージ: `glwrapper-ogl.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L43 — 未訳・API未接続

呼出: `mprf` / [原本 L43](../../crawl-ref/source/glwrapper-ogl.cc#L43)

### 原文（静的キー）

```text
Texture %s is bigger than maximum driver texture size (%d,%d vs. %d). Sprites from this texture will not display properly.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L98 — 未訳・API未接続

呼出: `mprf` / [原本 L98](../../crawl-ref/source/glwrapper-ogl.cc#L98)

### 原文（静的キー）

```text
OpenGL error %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L157 — 未訳・API未接続

呼出: `mprf` / [原本 L157](../../crawl-ref/source/glwrapper-ogl.cc#L157)

### 原文（静的キー）

```text
Mipmap Setup: Failed to load OpenGL version.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L172 — 未訳・API未接続

呼出: `mprf` / [原本 L172](../../crawl-ref/source/glwrapper-ogl.cc#L172)

### 原文（静的キー）

```text
Mipmap Setup: Disabled because OpenGL version: %s does not provide glGenerateMipmap.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L187 — 未訳・API未接続

呼出: `mprf` / [原本 L187](../../crawl-ref/source/glwrapper-ogl.cc#L187)

### 原文（静的キー）

```text
Mipmap Setup: Disabled because SDL_GL_LoadLibrary failed.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L197 — 未訳・API未接続

呼出: `mprf` / [原本 L197](../../crawl-ref/source/glwrapper-ogl.cc#L197)

### 原文（静的キー）

```text
Mipmap Setup: Failed to load glGenerateMipmap function.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L202 — 未訳・API未接続

呼出: `mprf` / [原本 L202](../../crawl-ref/source/glwrapper-ogl.cc#L202)

### 原文（静的キー）

```text
Mipmap Setup: success, loaded with OpenGL version: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L206 — 未訳・API未接続

呼出: `mprf` / [原本 L206](../../crawl-ref/source/glwrapper-ogl.cc#L206)

### 原文（静的キー）

```text
Mipmap Setup: skipped, not supported in this build configuration.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
