# コードメッセージ: `exclude.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L122 — 未訳・API未接続

呼出: `mprf` / [原本 L122](../../crawl-ref/source/exclude.cc#L122)

### 原文（静的キー）

```text
Marking area around %s as unsafe for travelling.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L171 — 未訳・API未接続

呼出: `mprf` / [原本 L171](../../crawl-ref/source/exclude.cc#L171)

### 原文（静的キー）

```text
exclusion not up-to-date: e (%d,%d) p (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
