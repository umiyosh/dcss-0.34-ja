# コードメッセージ: `mon-movetarget.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L68 — 未訳・API未接続

呼出: `mpr` / [原本 L68](../../crawl-ref/source/mon-movetarget.cc#L68)

### 原文（静的キー）

```text
No path found!
```

### 日本語

未訳（英語にフォールバック）。

## 2. L144 — 未訳・API未接続

呼出: `mprf` / [原本 L144](../../crawl-ref/source/mon-movetarget.cc#L144)

### 原文（静的キー）

```text
%s: Target out of reach! What now?
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L178 — 未訳・API未接続

呼出: `mprf` / [原本 L178](../../crawl-ref/source/mon-movetarget.cc#L178)

### 原文（静的キー）

```text
Need to calculate a path... (dist = %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L186 — 未訳・API未接続

呼出: `mprf` / [原本 L186](../../crawl-ref/source/mon-movetarget.cc#L186)

### 原文（静的キー）

```text
Distance too great, don't attempt pathfinding! (%s)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L193 — 未訳・API未接続

呼出: `mprf` / [原本 L193](../../crawl-ref/source/mon-movetarget.cc#L193)

### 原文（静的キー）

```text
Need a path for %s from (%d, %d) to (%d, %d), max. dist = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L319 — 未訳・API未接続

呼出: `mprf` / [原本 L319](../../crawl-ref/source/mon-movetarget.cc#L319)

### 原文（静的キー）

```text
merfolk avatar target is (%d, %d), dist = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L403 — 未訳・API未接続

呼出: `mprf` / [原本 L403](../../crawl-ref/source/mon-movetarget.cc#L403)

### 原文（静的キー）

```text
Found a path to (%d, %d) with %d surrounding water squares
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L420 — 未訳・API未接続

呼出: `mprf` / [原本 L420](../../crawl-ref/source/mon-movetarget.cc#L420)

### 原文（静的キー）

```text
Monster %s reached target (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L428 — 未訳・API未接続

呼出: `mpr` / [原本 L428](../../crawl-ref/source/mon-movetarget.cc#L428)

### 原文（静的キー）

```text
Arrived at first waypoint.
```

### 日本語

未訳（英語にフォールバック）。

## 10. L434 — 未訳・API未接続

呼出: `mpr` / [原本 L434](../../crawl-ref/source/mon-movetarget.cc#L434)

### 原文（静的キー）

```text
We reached the end of our path: stop travelling.
```

### 日本語

未訳（英語にフォールバック）。

## 11. L443 — 未訳・API未接続

呼出: `mprf` / [原本 L443](../../crawl-ref/source/mon-movetarget.cc#L443)

### 原文（静的キー）

```text
Next waypoint: (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L453 — 未訳・API未接続

呼出: `mpr` / [原本 L453](../../crawl-ref/source/mon-movetarget.cc#L453)

### 原文（静的キー）

```text
Can't see waypoint grid.
```

### 日本語

未訳（英語にフォールバック）。

## 13. L479 — 未訳・API未接続

呼出: `mprf` / [原本 L479](../../crawl-ref/source/mon-movetarget.cc#L479)

### 原文（静的キー）

```text
Need to erase %d of %d waypoints.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L502 — 未訳・API未接続

呼出: `mprf` / [原本 L502](../../crawl-ref/source/mon-movetarget.cc#L502)

### 原文（静的キー）

```text
Next waypoint: (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L687 — 未訳・API未接続

呼出: `mprf` / [原本 L687](../../crawl-ref/source/mon-movetarget.cc#L687)

### 原文（静的キー）

```text
Monster %s (pp: %d, %d) is now patrolling to (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
