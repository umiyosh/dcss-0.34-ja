# コードメッセージ: `dbg-scan.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L66 — 未訳・API未接続

呼出: `mprf` / [原本 L66](../../crawl-ref/source/dbg-scan.cc#L66)

### 原文（静的キー）

```text
env.igrid has invalid item index %d at (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L72 — 未訳・API未接続

呼出: `mprf` / [原本 L72](../../crawl-ref/source/dbg-scan.cc#L72)

### 原文（静的キー）

```text
Item in stack at (%d, %d) has invalid link %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L97 — 未訳・API未接続

呼出: `mprf` / [原本 L97](../../crawl-ref/source/dbg-scan.cc#L97)

### 原文（静的キー）

```text
Potential INFINITE STACK at (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L127 — 未訳・API未接続

呼出: `mprf` / [原本 L127](../../crawl-ref/source/dbg-scan.cc#L127)

### 原文（静的キー）

```text
Item position (%d, %d) is out of bounds
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L132 — 未訳・API未接続

呼出: `mprf` / [原本 L132](../../crawl-ref/source/dbg-scan.cc#L132)

### 原文（静的キー）

```text
env.igrid(%d,%d) = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L144 — 未訳・API未接続

呼出: `mprf` / [原本 L144](../../crawl-ref/source/dbg-scan.cc#L144)

### 原文（静的キー）

```text
Held by monster #%d: %s at (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L199 — 未訳・API未接続

呼出: `mprf` / [原本 L199](../../crawl-ref/source/dbg-scan.cc#L199)

### 原文（静的キー）

```text
Program bug detected!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L200 — 未訳・API未接続

呼出: `mprf` / [原本 L200](../../crawl-ref/source/dbg-scan.cc#L200)

### 原文（静的キー）

```text
Buggy monster detected: monster #%d; position (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L214 — 未訳・API未接続

呼出: `mprf` / [原本 L214](../../crawl-ref/source/dbg-scan.cc#L214)

### 原文（静的キー）

```text
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L215 — 未訳・API未接続

呼出: `mprf` / [原本 L215](../../crawl-ref/source/dbg-scan.cc#L215)

### 原文（静的キー）

```text
env.mgrid problem occurred during level generation
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L283 — 未訳・API未接続

呼出: `mprf` / [原本 L283](../../crawl-ref/source/dbg-scan.cc#L283)

### 原文（静的キー）

```text
env.mgrid at (%d, %d) has invalid monster index %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L297 — 未訳・API未接続

呼出: `mprf` / [原本 L297](../../crawl-ref/source/dbg-scan.cc#L297)

### 原文（静的キー）

```text
Bogosity: env.mgrid at (%d,%d) points at %s, but monster is at (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L303 — 未訳・API未接続

呼出: `mprf` / [原本 L303](../../crawl-ref/source/dbg-scan.cc#L303)

### 原文（静的キー）

```text
Additionally, it isn't alive.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L335 — 未訳・API未接続

呼出: `mprf` / [原本 L335](../../crawl-ref/source/dbg-scan.cc#L335)

### 原文（静的キー）

```text
Bogus monster type %d at (%d, %d), midx = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L341 — 未訳・API未接続

呼出: `mprf` / [原本 L341](../../crawl-ref/source/dbg-scan.cc#L341)

### 原文（静的キー）

```text
Out of bounds monster: %s at (%d, %d), midx = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L352 — 未訳・API未接続

呼出: `mprf` / [原本 L352](../../crawl-ref/source/dbg-scan.cc#L352)

### 原文（静的キー）

```text
Floating monster: %s at (%d,%d), midx = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L369 — 未訳・API未接続

呼出: `mprf` / [原本 L369](../../crawl-ref/source/dbg-scan.cc#L369)

### 原文（静的キー）

```text
Also at (%d, %d): %s, midx = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L374 — 未訳・API未接続

呼出: `mprf` / [原本 L374](../../crawl-ref/source/dbg-scan.cc#L374)

### 原文（静的キー）

```text
Dead mon also at (%d, %d): %s,midx = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L387 — 未訳・API未接続

呼出: `mprf` / [原本 L387](../../crawl-ref/source/dbg-scan.cc#L387)

### 原文（静的キー）

```text
Monster %s in %s at (%d, %d)%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L402 — 未訳・API未接続

呼出: `mprf` / [原本 L402](../../crawl-ref/source/dbg-scan.cc#L402)

### 原文（静的キー）

```text
Monster %s (%d, %d) has invalid item index %d in slot %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 21. L414 — 未訳・API未接続

呼出: `mprf` / [原本 L414](../../crawl-ref/source/dbg-scan.cc#L414)

### 原文（静的キー）

```text
Monster %s (%d, %d) holding invalid item in slot %d (midx = %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 22. L440 — 未訳・API未接続

呼出: `mprf` / [原本 L440](../../crawl-ref/source/dbg-scan.cc#L440)

### 原文（静的キー）

```text
Monster %s (%d, %d) [midx = %d] holding item %s, but item thinks it's held by monster %s (%d, %d) [midx = %d]
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 23. L454 — 未訳・API未接続

呼出: `mprf` / [原本 L454](../../crawl-ref/source/dbg-scan.cc#L454)

### 原文（静的キー）

```text
Other monster thinks it's holding the item, too.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L460 — 未訳・API未接続

呼出: `mprf` / [原本 L460](../../crawl-ref/source/dbg-scan.cc#L460)

### 原文（静的キー）

```text
Other monster isn't holding it, though.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L471 — 未訳・API未接続

呼出: `mprf` / [原本 L471](../../crawl-ref/source/dbg-scan.cc#L471)

### 原文（静的キー）

```text
Error: monster %s(%d) has same mid as %s(%d) (%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L482 — 未訳・API未接続

呼出: `mprf` / [原本 L482](../../crawl-ref/source/dbg-scan.cc#L482)

### 原文（静的キー）

```text
Error: constricting[you] entry missing for monster %s(%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 27. L491 — 未訳・API未接続

呼出: `mprf` / [原本 L491](../../crawl-ref/source/dbg-scan.cc#L491)

### 原文（静的キー）

```text
Error: constrictor missing for monster %s(%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 28. L496 — 未訳・API未接続

呼出: `mprf` / [原本 L496](../../crawl-ref/source/dbg-scan.cc#L496)

### 原文（静的キー）

```text
Error: constricting[%s(mindex=%d mid=%d)] entry missing for monster %s(mindex=%d mid=%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 29. L521 — 未訳・API未接続

呼出: `mprf` / [原本 L521](../../crawl-ref/source/dbg-scan.cc#L521)

### 原文（静的キー）

```text
Error: player on same spot as monster: %s(%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 30. L541 — 未訳・API未接続

呼出: `mprf` / [原本 L541](../../crawl-ref/source/dbg-scan.cc#L541)

### 原文（静的キー）

```text
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 31. L547 — 未訳・API未接続

呼出: `mpr` / [原本 L547](../../crawl-ref/source/dbg-scan.cc#L547)

### 原文（静的キー）

```text

```

### 日本語

未訳（英語にフォールバック）。

## 32. L559 — 未訳・API未接続

呼出: `mprf` / [原本 L559](../../crawl-ref/source/dbg-scan.cc#L559)

### 原文（静的キー）

```text
%s not in any vaults.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 33. L567 — 未訳・API未接続

呼出: `mpr` / [原本 L567](../../crawl-ref/source/dbg-scan.cc#L567)

### 原文（静的キー）

```text

```

### 日本語

未訳（英語にフォールバック）。

## 34. L581 — 未訳・API未接続

呼出: `mprf` / [原本 L581](../../crawl-ref/source/dbg-scan.cc#L581)

### 原文（静的キー）

```text
%s not in any vaults.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 35. L596 — 未訳・API未接続

呼出: `mprf` / [原本 L596](../../crawl-ref/source/dbg-scan.cc#L596)

### 原文（静的キー）

```text
%s not in any vaults.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 36. L604 — 未訳・API未接続

呼出: `mprf` / [原本 L604](../../crawl-ref/source/dbg-scan.cc#L604)

### 原文（静的キー）

```text
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
