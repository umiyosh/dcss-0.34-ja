# コードメッセージ: `mapdef.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L204 — 未訳・API未接続

呼出: `mprf` / [原本 L204](../../crawl-ref/source/mapdef.cc#L204)

### 原文（静的キー）

```text
adding %s on index %d (%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L2102 — 未訳・API未接続

呼出: `mprf` / [原本 L2102](../../crawl-ref/source/mapdef.cc#L2102)

### 原文（静的キー）

```text
dgn_set_map failed for '%s': %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L2113 — 未訳・API未接続

呼出: `mprf` / [原本 L2113](../../crawl-ref/source/mapdef.cc#L2113)

### 原文（静的キー）

```text
dgn_set_map failed: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L2634 — 未訳・API未接続

呼出: `mprf` / [原本 L2634](../../crawl-ref/source/mapdef.cc#L2634)

### 原文（静的キー）

```text
Lua error copying hook (%s) from '%s' to '%s': %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L2659 — 未訳・API未接続

呼出: `mprf` / [原本 L2659](../../crawl-ref/source/mapdef.cc#L2659)

### 原文（静的キー）

```text
Lua error running hook '%s' on map '%s': %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L2687 — 未訳・API未接続

呼出: `mprf` / [原本 L2687](../../crawl-ref/source/mapdef.cc#L2687)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L2701 — 未訳・API未接続

呼出: `mprf` / [原本 L2701](../../crawl-ref/source/mapdef.cc#L2701)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L4457 — 未訳・API未接続

呼出: `mprf` / [原本 L4457](../../crawl-ref/source/mapdef.cc#L4457)

### 原文（静的キー）

```text
Hydra spec wants %d heads, clamping to 20.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L4485 — 未訳・API未接続

呼出: `mprf` / [原本 L4485](../../crawl-ref/source/mapdef.cc#L4485)

### 原文（静的キー）

```text
Slime spec wants invalid size '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
