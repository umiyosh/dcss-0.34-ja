# コードメッセージ: `maps.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L199 — 未訳・API未接続

呼出: `mprf` / [原本 L199](../../crawl-ref/source/maps.cc#L199)

### 原文（静的キー）

```text
Fatal lua error%s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L204 — 未訳・API未接続

呼出: `mprf` / [原本 L204](../../crawl-ref/source/maps.cc#L204)

### 原文（静的キー）

```text

%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L603 — 未訳・API未接続

呼出: `mprf` / [原本 L603](../../crawl-ref/source/maps.cc#L603)

### 原文（静的キー）

```text
Skipping (%d,%d): not safe to place map
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L613 — 未訳・API未接続

呼出: `mprf` / [原本 L613](../../crawl-ref/source/maps.cc#L613)

### 原文（静的キー）

```text
Skipping (%d,%d): not a good minivault place (tags: %s)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L1502 — 未訳・API未接続

呼出: `mprf` / [原本 L1502](../../crawl-ref/source/maps.cc#L1502)

### 原文（静的キー）

```text
Regenerating des: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L1584 — 未訳・API未接続

呼出: `mprf` / [原本 L1584](../../crawl-ref/source/maps.cc#L1584)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L1604 — 未訳・API未接続

呼出: `mprf` / [原本 L1604](../../crawl-ref/source/maps.cc#L1604)

### 原文（静的キー）

```text
Lua error (map %s)%s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L1608 — 未訳・API未接続

呼出: `mprf` / [原本 L1608](../../crawl-ref/source/maps.cc#L1608)

### 原文（静的キー）

```text

%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
