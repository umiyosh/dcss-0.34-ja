# コードメッセージ: `player-stats.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L85 — 未訳・API未接続

呼出: `mprf` / [原本 L85](../../crawl-ref/source/player-stats.cc#L85)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L91 — 未訳・API未接続

呼出: `mprf` / [原本 L91](../../crawl-ref/source/player-stats.cc#L91)

### 原文（静的キー）

```text
Your base attributes are Str %d, Int %d, Dex %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L96 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L96](../../crawl-ref/source/player-stats.cc#L96)

### 未解決の式（断片を翻訳キーにしない）

```text
need_caps
        ? "Increase (S)trength, (I)ntelligence, or (D)exterity? "
        : "Increase (s)trength, (i)ntelligence, or (d)exterity? "
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L155 — 未訳・API未接続

呼出: `mprf` / [原本 L155](../../crawl-ref/source/player-stats.cc#L155)

### 原文（静的キー）

```text
Uppercase letters only, please.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L186 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L186](../../crawl-ref/source/player-stats.cc#L186)

### 未解決の式（断片を翻訳キーにしない）

```text
(amount > 0) ? MSGCH_INTRINSIC_GAIN : MSGCH_WARN
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L209 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L209](../../crawl-ref/source/player-stats.cc#L209)

### 未解決の式（断片を翻訳キーにしない）

```text
(amount > 0) ? MSGCH_INTRINSIC_GAIN : MSGCH_WARN
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L395 — 未訳・API未接続

呼出: `mprf` / [原本 L395](../../crawl-ref/source/player-stats.cc#L395)

### 原文（静的キー）

```text
Bad stat: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L410 — 未訳・API未接続

呼出: `mprf` / [原本 L410](../../crawl-ref/source/player-stats.cc#L410)

### 原文（静的キー）

```text
You have lost all your %s. It will be difficult to act quickly in this state!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L417 — 未訳・API未接続

呼出: `mprf` / [原本 L417](../../crawl-ref/source/player-stats.cc#L417)

### 原文（静的キー）

```text
You have recovered your %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
