# コードメッセージ: `mon-pathfind.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L107 — 未訳・API未接続

呼出: `mpr` / [原本 L107](../../crawl-ref/source/mon-pathfind.cc#L107)

### 原文（静的キー）

```text
The monster is already there!
```

### 日本語

未訳（英語にフォールバック）。

## 2. L193 — 未訳・API未接続

呼出: `mprf` / [原本 L193](../../crawl-ref/source/mon-pathfind.cc#L193)

### 原文（静的キー）

```text
Couldn't find a path from (%d,%d) to (%d,%d).
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L232 — 未訳・API未接続

呼出: `mprf` / [原本 L232](../../crawl-ref/source/mon-pathfind.cc#L232)

### 原文（静的キー）

```text
Looking at neighbour (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L258 — 未訳・API未接続

呼出: `mprf` / [原本 L258](../../crawl-ref/source/mon-pathfind.cc#L258)

### 原文（静的キー）

```text
old dist: %d, new dist: %d, infinite: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L270 — 未訳・API未接続

呼出: `mprf` / [原本 L270](../../crawl-ref/source/mon-pathfind.cc#L270)

### 原文（静的キー）

```text
Adding (%d,%d) to hash (total dist = %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L280 — 未訳・API未接続

呼出: `mprf` / [原本 L280](../../crawl-ref/source/mon-pathfind.cc#L280)

### 原文（静的キー）

```text
Improving (%d,%d) to total dist %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L302 — 未訳・API未接続

呼出: `mpr` / [原本 L302](../../crawl-ref/source/mon-pathfind.cc#L302)

### 原文（静的キー）

```text
Arrived at target.
```

### 日本語

未訳（英語にフォールバック）。

## 8. L330 — 未訳・API未接続

呼出: `mprf` / [原本 L330](../../crawl-ref/source/mon-pathfind.cc#L330)

### 原文（静的キー）

```text
Returning (%d, %d) as best pos with total dist %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L337 — 未訳・API未接続

呼出: `mprf` / [原本 L337](../../crawl-ref/source/mon-pathfind.cc#L337)

### 原文（静的キー）

```text
No positions for path length %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L350 — 未訳・API未接続

呼出: `mpr` / [原本 L350](../../crawl-ref/source/mon-pathfind.cc#L350)

### 原文（静的キー）

```text
Backtracking...
```

### 日本語

未訳（英語にフォールバック）。

## 11. L366 — 未訳・API未接続

呼出: `mprf` / [原本 L366](../../crawl-ref/source/mon-pathfind.cc#L366)

### 原文（静的キー）

```text
prev: (%d, %d), pos: (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L399 — 未訳・API未接続

呼出: `mpr` / [原本 L399](../../crawl-ref/source/mon-pathfind.cc#L399)

### 原文（静的キー）

```text

Waypoints:
```

### 日本語

未訳（英語にフォールバック）。

## 13. L410 — 未訳・API未接続

呼出: `mprf` / [原本 L410](../../crawl-ref/source/mon-pathfind.cc#L410)

### 原文（静的キー）

```text
waypoint: (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
