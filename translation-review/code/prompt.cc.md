# コードメッセージ: `prompt.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L88 — 未訳・API未接続

呼出: `mprf` / [原本 L88](../../crawl-ref/source/prompt.cc#L88)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L118 — 未訳・API未接続

呼出: `mprf` / [原本 L118](../../crawl-ref/source/prompt.cc#L118)

### 原文（静的キー）

```json
"%s (Confirm with \"%s\".) "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L248 — 未訳・API未接続

呼出: `mprf` / [原本 L248](../../crawl-ref/source/prompt.cc#L248)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L300 — 未訳・API未接続

呼出: `mprf` / [原本 L300](../../crawl-ref/source/prompt.cc#L300)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L543 — 未訳・API未接続

呼出: `mprf` / [原本 L543](../../crawl-ref/source/prompt.cc#L543)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
