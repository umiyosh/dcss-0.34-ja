# コードメッセージ: `wiz-dump.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L492 — 未訳・API未接続

呼出: `mprf` / [原本 L492](../../crawl-ref/source/wiz-dump.cc#L492)

### 原文（静的キー）

```text
Unknown species: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L498 — 未訳・API未接続

呼出: `mprf` / [原本 L498](../../crawl-ref/source/wiz-dump.cc#L498)

### 原文（静的キー）

```text
Unknown job: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L556 — 未訳・API未接続

呼出: `mprf` / [原本 L556](../../crawl-ref/source/wiz-dump.cc#L556)

### 原文（静的キー）

```text
unknown item: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
