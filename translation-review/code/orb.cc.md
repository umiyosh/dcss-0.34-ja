# コードメッセージ: `orb.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L65 — 未訳・API未接続

呼出: `mprf` / [原本 L65](../../crawl-ref/source/orb.cc#L65)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L67 — 未訳・API未接続

呼出: `mprf` / [原本 L67](../../crawl-ref/source/orb.cc#L67)

### 原文（静的キー）

```text
The Orb lets out an agonising shriek!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L72 — 未訳・API未接続

呼出: `mprf` / [原本 L72](../../crawl-ref/source/orb.cc#L72)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L74 — 未訳・API未接続

呼出: `mprf` / [原本 L74](../../crawl-ref/source/orb.cc#L74)

### 原文（静的キー）

```text
The Orb lets out a furious burst of light!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L93 — 未訳・API未接続

呼出: `mprf` / [原本 L93](../../crawl-ref/source/orb.cc#L93)

### 原文（静的キー）

```text
The lords of Pandemonium are not amused. Beware!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L95 — 未訳・API未接続

呼出: `simple_god_message` / [原本 L95](../../crawl-ref/source/orb.cc#L95)

### 原文（静的キー）

```text
 tells them not to hurry.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 7. L98 — 未訳・API未接続

呼出: `mprf` / [原本 L98](../../crawl-ref/source/orb.cc#L98)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
