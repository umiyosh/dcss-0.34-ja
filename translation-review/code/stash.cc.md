# コードメッセージ: `stash.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L135 — 未訳・API未接続

呼出: `mprf` / [原本 L135](../../crawl-ref/source/stash.cc#L135)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L1411 — 未訳・API未接続

呼出: `mprf` / [原本 L1411](../../crawl-ref/source/stash.cc#L1411)

### 原文（静的キー）

```text
Your search expression is invalid.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L1426 — 未訳・API未接続

呼出: `mprf` / [原本 L1426](../../crawl-ref/source/stash.cc#L1426)

### 原文（静的キー）

```text
Can't find anything matching that.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L1435 — 未訳・API未接続

呼出: `mprf` / [原本 L1435](../../crawl-ref/source/stash.cc#L1435)

### 原文（静的キー）

```text
Too many matches; use a more specific search.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
