# コードメッセージ: `spl-monench.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L51 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L51](../../crawl-ref/source/spl-monench.cc#L51)

### 原文（静的キー）

```text
 is unaffected.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 2. L63 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L63](../../crawl-ref/source/spl-monench.cc#L63)

### 原文（静的キー）

```text
 resists.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 3. L85 — 未訳・API未接続

呼出: `mpr` / [原本 L85](../../crawl-ref/source/spl-monench.cc#L85)

### 原文（静的キー）

```text
You radiate an aura of cold.
```

### 日本語

未訳（英語にフォールバック）。

## 4. L105 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L105](../../crawl-ref/source/spl-monench.cc#L105)

### 原文（静的キー）

```text
 is outlined in light.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 5. L107 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L107](../../crawl-ref/source/spl-monench.cc#L107)

### 原文（静的キー）

```text
 glows brighter for a moment.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 6. L120 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L120](../../crawl-ref/source/spl-monench.cc#L120)

### 原文（静的キー）

```text
 seems to slow down.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 7. L133 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L133](../../crawl-ref/source/spl-monench.cc#L133)

### 原文（静的キー）

```text
 loses the ability to speak.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 8. L158 — 動的・未対応式（要調査・要改修）

呼出: `simple_monster_message` / [原本 L158](../../crawl-ref/source/spl-monench.cc#L158)

### 未解決の式（断片を翻訳キーにしない）

```text
mon.resist_margin_phrase(res_margin).c_str()
```

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 9. L174 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L174](../../crawl-ref/source/spl-monench.cc#L174)

### 原文（静的キー）

```text
 partially resists.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 10. L176 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L176](../../crawl-ref/source/spl-monench.cc#L176)

### 原文（静的キー）

```text
 is enfeebled!
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 11. L217 — 未訳・API未接続

呼出: `mpr` / [原本 L217](../../crawl-ref/source/spl-monench.cc#L217)

### 原文（静的キー）

```text
You are enfeebled!
```

### 日本語

未訳（英語にフォールバック）。

## 12. L245 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L245](../../crawl-ref/source/spl-monench.cc#L245)

### 未解決の式（断片を翻訳キーにしない）

```text
target.is_player() ? MSGCH_WARN : MSGCH_PLAIN
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L303 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L303](../../crawl-ref/source/spl-monench.cc#L303)

### 原文（静的キー）

```text
 is afflicted with rimeblight.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 14. L333 — 未訳・API未接続

呼出: `mprf` / [原本 L333](../../crawl-ref/source/spl-monench.cc#L333)

### 原文（静的キー）

```text
Shards of ice erupt from %s body!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L387 — 未訳・API未接続

呼出: `mprf` / [原本 L387](../../crawl-ref/source/spl-monench.cc#L387)

### 原文（静的キー）

```text
The sign of ruin forms upon you!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L393 — 未訳・API未接続

呼出: `mprf` / [原本 L393](../../crawl-ref/source/spl-monench.cc#L393)

### 原文（静的キー）

```text
The sign of ruin forms upon %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L410 — 未訳・API未接続

呼出: `mprf` / [原本 L410](../../crawl-ref/source/spl-monench.cc#L410)

### 原文（静的キー）

```text
A magical hammer augments %s in a flurry of sparks and slag.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L506 — 未訳・API未接続

呼出: `mprf` / [原本 L506](../../crawl-ref/source/spl-monench.cc#L506)

### 原文（静的キー）

```text
%s %s %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L531 — 未訳・API未接続

呼出: `mprf` / [原本 L531](../../crawl-ref/source/spl-monench.cc#L531)

### 原文（静的キー）

```text
%s glares at %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L670 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L670](../../crawl-ref/source/spl-monench.cc#L670)

### 原文（静的キー）

```text
 is enveloped in gloom.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。
