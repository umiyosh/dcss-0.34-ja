# コードメッセージ: `item-prop.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L3337 — 未訳・API未接続

呼出: `mprf` / [原本 L3337](../../crawl-ref/source/item-prop.cc#L3337)

### 原文（静的キー）

```text
You learned that %s %s actually %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L3470 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L3470](../../crawl-ref/source/item-prop.cc#L3470)

### 未解決の式（断片を翻訳キーにしない）

```text
_xp_evoker_recharge_msg(evoker, gained, silenced).c_str()
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
