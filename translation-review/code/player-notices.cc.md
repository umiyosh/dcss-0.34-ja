# コードメッセージ: `player-notices.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L78 — 未訳・API未接続

呼出: `mprf` / [原本 L78](../../crawl-ref/source/player-notices.cc#L78)

### 原文（静的キー）

```text
%s comes into view.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L430 — 未訳・API未接続

呼出: `mprf` / [原本 L430](../../crawl-ref/source/player-notices.cc#L430)

### 原文（静的キー）

```text
%s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L449 — 未訳・API未接続

呼出: `mprf` / [原本 L449](../../crawl-ref/source/player-notices.cc#L449)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L640 — 未訳・API未接続

呼出: `mpr` / [原本 L640](../../crawl-ref/source/player-notices.cc#L640)

### 原文（静的キー）

```text
<green>Wyrmbane glows as a worthy foe approaches.</green>
```

### 日本語

未訳（英語にフォールバック）。

## 5. L682 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L682](../../crawl-ref/source/player-notices.cc#L682)

### 未解決の式（断片を翻訳キーにしない）

```text
GOD_GOZAG
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L694 — 動的・未対応式（要調査・要改修）

呼出: `simple_god_message` / [原本 L694](../../crawl-ref/source/player-notices.cc#L694)

### 未解決の式（断片を翻訳キーにしない）

```text
msg.c_str()
```

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。
