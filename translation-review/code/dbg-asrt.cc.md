# コードメッセージ: `dbg-asrt.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L403 — 未訳・API未接続

呼出: `mprf` / [原本 L403](../../crawl-ref/source/dbg-asrt.cc#L403)

### 原文（静的キー）

```text
Marker #%d is nullptr
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L411 — 未訳・API未接続

呼出: `mprf` / [原本 L411](../../crawl-ref/source/dbg-asrt.cc#L411)

### 原文（静的キー）

```text
Marker #%d at (%d, %d) has invalid type %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L417 — 未訳・API未接続

呼出: `mprf` / [原本 L417](../../crawl-ref/source/dbg-asrt.cc#L417)

### 原文（静的キー）

```text
Marker #%d, type %d at (%d, %d) out of bounds
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L425 — 未訳・API未接続

呼出: `mprf` / [原本 L425](../../crawl-ref/source/dbg-asrt.cc#L425)

### 原文（静的キー）

```text
Marker #%d, type %d at (%d, %d) unlinked
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L442 — 未訳・API未接続

呼出: `mprf` / [原本 L442](../../crawl-ref/source/dbg-asrt.cc#L442)

### 原文（静的キー）

```text
Marker #%d at (%d, %d) nullptr
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L448 — 未訳・API未接続

呼出: `mprf` / [原本 L448](../../crawl-ref/source/dbg-asrt.cc#L448)

### 原文（静的キー）

```text
Marker #%d, type %d at (%d, %d) thinks it's at (%d, %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L454 — 未訳・API未接続

呼出: `mprf` / [原本 L454](../../crawl-ref/source/dbg-asrt.cc#L454)

### 原文（静的キー）

```text
Further, it thinks it's out of bounds.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L471 — 未訳・API未接続

呼出: `mprf` / [原本 L471](../../crawl-ref/source/dbg-asrt.cc#L471)

### 原文（静的キー）

```text
Marker #%d, type %d at (%d, %d): %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
