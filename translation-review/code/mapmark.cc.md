# コードメッセージ: `mapmark.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L189 — 未訳・API未接続

呼出: `mprf` / [原本 L189](../../crawl-ref/source/mapmark.cc#L189)

### 原文（静的キー）

```text
lua_marker exec error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L202 — 未訳・API未接続

呼出: `mprf` / [原本 L202](../../crawl-ref/source/mapmark.cc#L202)

### 原文（静的キー）

```text
lua_marker load error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L204 — 未訳・API未接続

呼出: `mprf` / [原本 L204](../../crawl-ref/source/mapmark.cc#L204)

### 原文（静的キー）

```text
lua_marker exec error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L210 — 未訳・API未接続

呼出: `mprf` / [原本 L210](../../crawl-ref/source/mapmark.cc#L210)

### 原文（静的キー）

```text
lua_marker_mapless exec error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L234 — 未訳・API未接続

呼出: `mprf` / [原本 L234](../../crawl-ref/source/mapmark.cc#L234)

### 原文（静的キー）

```text
lua_marker: Expected table, didn't get it.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L263 — 未訳・API未接続

呼出: `mprf` / [原本 L263](../../crawl-ref/source/mapmark.cc#L263)

### 原文（静的キー）

```text
Couldn't find table.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L340 — 未訳・API未接続

呼出: `mprf` / [原本 L340](../../crawl-ref/source/mapmark.cc#L340)

### 原文（静的キー）

```text
mlua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L366 — 未訳・API未接続

呼出: `mprf` / [原本 L366](../../crawl-ref/source/mapmark.cc#L366)

### 原文（静的キー）

```text
notify_dgn_event: Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L406 — 未訳・API未接続

呼出: `mprf` / [原本 L406](../../crawl-ref/source/mapmark.cc#L406)

### 原文（静的キー）

```text
Lua marker property (%s) error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
