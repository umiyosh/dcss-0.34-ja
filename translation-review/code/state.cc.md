# コードメッセージ: `state.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L145 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L145](../../crawl-ref/source/state.cc#L145)

### 未解決の式（断片を翻訳キーにしない）

```text
reason
```

## 2. L162 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L162](../../crawl-ref/source/state.cc#L162)

### 未解決の式（断片を翻訳キーにしない）

```text
reason
```
