# コードメッセージ: `attitude-change.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L169 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L169](../../crawl-ref/source/attitude-change.cc#L169)

### 未解決の式（断片を翻訳キーにしない）

```text
channel
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L272 — 未訳・API未接続

呼出: `mprf` / [原本 L272](../../crawl-ref/source/attitude-change.cc#L272)

### 原文（静的キー）

```text
%s stares at you suspiciously for a moment, then relaxes.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L279 — 未訳・API未接続

呼出: `mprf` / [原本 L279](../../crawl-ref/source/attitude-change.cc#L279)

### 原文（静的キー）

```text
%s trembles before you.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L367 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L367](../../crawl-ref/source/attitude-change.cc#L367)

### 未解決の式（断片を翻訳キーにしない）

```text
channel
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
