# コードメッセージ: `startup.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L76 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L76](../../crawl-ref/source/startup.cc#L76)

### 未解決の式（断片を翻訳キーにしない）

```text
m.c_str()
```

## 2. L209 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L209](../../crawl-ref/source/startup.cc#L209)

### 未解決の式（断片を翻訳キーにしない）

```text
opening_screen().tostring().c_str()
```

## 3. L210 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L210](../../crawl-ref/source/startup.cc#L210)

### 未解決の式（断片を翻訳キーにしない）

```text
options_read_status().tostring().c_str()
```
