# コードメッセージ: `attack.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L671 — 未訳・API未接続

呼出: `mprf` / [原本 L671](../../crawl-ref/source/attack.cc#L671)

### 原文（静的キー）

```text
%s %s %s vigour!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L750 — 未訳・API未接続

呼出: `mprf` / [原本 L750](../../crawl-ref/source/attack.cc#L750)

### 原文（静的キー）

```text
You catch %s completely off-guard!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L755 — 未訳・API未接続

呼出: `mprf` / [原本 L755](../../crawl-ref/source/attack.cc#L755)

### 原文（静的キー）

```text
You %s %s from behind!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L763 — 未訳・API未接続

呼出: `mprf` / [原本 L763](../../crawl-ref/source/attack.cc#L763)

### 原文（静的キー）

```text
You pounce on the unaware %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L767 — 未訳・API未接続

呼出: `mprf` / [原本 L767](../../crawl-ref/source/attack.cc#L767)

### 原文（静的キー）

```text
%s fails to defend %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L1247 — 未訳・API未接続

呼出: `mprf` / [原本 L1247](../../crawl-ref/source/attack.cc#L1247)

### 原文（静的キー）

```text
%s draws strength from your wounds!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L1252 — 未訳・API未接続

呼出: `mprf` / [原本 L1252](../../crawl-ref/source/attack.cc#L1252)

### 原文（静的キー）

```text
%s is healed.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L1320 — 未訳・API未接続

呼出: `mprf` / [原本 L1320](../../crawl-ref/source/attack.cc#L1320)

### 原文（静的キー）

```text
%s is completely immune to your confusing touch!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L1353 — 未訳・API未接続

呼出: `mprf` / [原本 L1353](../../crawl-ref/source/attack.cc#L1353)

### 原文（静的キー）

```text
%s becomes entangled by vines.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L1380 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L1380](../../crawl-ref/source/attack.cc#L1380)

### 未解決の式（断片を翻訳キーにしない）

```text
special_damage_message
```

## 11. L1471 — 未訳・API未接続

呼出: `mprf` / [原本 L1471](../../crawl-ref/source/attack.cc#L1471)

### 原文（静的キー）

```text
You feel devious.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L1618 — 未訳・API未接続

呼出: `mpr` / [原本 L1618](../../crawl-ref/source/attack.cc#L1618)

### 原文（静的キー）

```text
Your autodazzler retaliates!
```

### 日本語

未訳（英語にフォールバック）。

## 13. L1630 — 未訳・API未接続

呼出: `mprf` / [原本 L1630](../../crawl-ref/source/attack.cc#L1630)

### 原文（静的キー）

```text
Your paragon deflects %s attack away from you.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
