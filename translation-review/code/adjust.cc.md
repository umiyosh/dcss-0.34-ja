# コードメッセージ: `adjust.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L30 — 未訳・API未接続

呼出: `mprf` / [原本 L30](../../crawl-ref/source/adjust.cc#L30)

### 原文（静的キー）

```json
"Adjust (g)ear, (s)pells, (a)bilities, (p)otions, sc(r)olls or e(v)ocables? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L112 — 未訳・API未接続

呼出: `mprf` / [原本 L112](../../crawl-ref/source/adjust.cc#L112)

### 原文（静的キー）

```json
"Adjust which spell? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L127 — 未訳・API未接続

呼出: `mpr` / [原本 L127](../../crawl-ref/source/adjust.cc#L127)

### 原文（静的キー）

```text
You don't know that spell.
```

### 日本語

未訳（英語にフォールバック）。

## 4. L138 — 未訳・API未接続

呼出: `mprf` / [原本 L138](../../crawl-ref/source/adjust.cc#L138)

### 原文（静的キー）

```json
"Adjust to which letter? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L186 — 未訳・API未接続

呼出: `mpr` / [原本 L186](../../crawl-ref/source/adjust.cc#L186)

### 原文（静的キー）

```text
You don't currently have any abilities.
```

### 日本語

未訳（英語にフォールバック）。

## 6. L190 — 未訳・API未接続

呼出: `mprf` / [原本 L190](../../crawl-ref/source/adjust.cc#L190)

### 原文（静的キー）

```json
"Adjust which ability? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L196 — 未訳・API未接続

呼出: `mpr` / [原本 L196](../../crawl-ref/source/adjust.cc#L196)

### 原文（静的キー）

```text
No such ability.
```

### 日本語

未訳（英語にフォールバック）。

## 8. L206 — 未訳・API未接続

呼出: `mprf` / [原本 L206](../../crawl-ref/source/adjust.cc#L206)

### 原文（静的キー）

```text
Adjust to which letter?
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
