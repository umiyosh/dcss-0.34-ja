# コードメッセージ: `wiz-dgn.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L63 — 未訳・API未接続

呼出: `mpr` / [原本 L63](../../crawl-ref/source/wiz-dgn.cc#L63)

### 原文（静的キー）

```text
Can't go down from the bottom of a branch.
```

### 日本語

未訳（英語にフォールバック）。

## 2. L79 — 未訳・API未接続

呼出: `mpr` / [原本 L79](../../crawl-ref/source/wiz-dgn.cc#L79)

### 原文（静的キー）

```text
Bug in determining level exit.
```

### 日本語

未訳（英語にフォールバック）。

## 3. L91 — 未訳・API未接続

呼出: `mprf` / [原本 L91](../../crawl-ref/source/wiz-dgn.cc#L91)

### 原文（静的キー）

```text
Creating %sstairs.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L200 — 未訳・API未接続

呼出: `mprf` / [原本 L200](../../crawl-ref/source/wiz-dgn.cc#L200)

### 原文（静的キー）

```json
"Create what kind of feature mimic? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L202 — 未訳・API未接続

呼出: `mprf` / [原本 L202](../../crawl-ref/source/wiz-dgn.cc#L202)

### 原文（静的キー）

```json
"Create which feature? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L230 — 未訳・API未接続

呼出: `mprf` / [原本 L230](../../crawl-ref/source/wiz-dgn.cc#L230)

### 原文（静的キー）

```text
Set fprops "%s" at (%d,%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L235 — 未訳・API未接続

呼出: `mprf` / [原本 L235](../../crawl-ref/source/wiz-dgn.cc#L235)

### 原文（静的キー）

```text
No features matching '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L381 — 未訳・API未接続

呼出: `mprf` / [原本 L381](../../crawl-ref/source/wiz-dgn.cc#L381)

### 原文（静的キー）

```text
Branch %d (%s) is on %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L386 — 未訳・API未接続

呼出: `mprf` / [原本 L386](../../crawl-ref/source/wiz-dgn.cc#L386)

### 原文（静的キー）

```text
Branch %d (%s) was not generated this game
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L394 — 未訳・API未接続

呼出: `mprf` / [原本 L394](../../crawl-ref/source/wiz-dgn.cc#L394)

### 原文（静的キー）

```text
----
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L395 — 未訳・API未接続

呼出: `mprf` / [原本 L395](../../crawl-ref/source/wiz-dgn.cc#L395)

### 原文（静的キー）

```json
"Overflow temples: "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L432 — 未訳・API未接続

呼出: `mprf` / [原本 L432](../../crawl-ref/source/wiz-dgn.cc#L432)

### 原文（静的キー）

```text
%u on D:%u (%s)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L452 — 未訳・API未接続

呼出: `mpr` / [原本 L452](../../crawl-ref/source/wiz-dgn.cc#L452)

### 原文（静的キー）

```text
Mapping level.
```

### 日本語

未訳（英語にフォールバック）。

## 14. L474 — 未訳・API未接続

呼出: `mprf` / [原本 L474](../../crawl-ref/source/wiz-dgn.cc#L474)

### 原文（静的キー）

```text
You can only make a %s on a floor square.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L498 — 未訳・API未接続

呼出: `mprf` / [原本 L498](../../crawl-ref/source/wiz-dgn.cc#L498)

### 原文（静的キー）

```text
Created %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L504 — 未訳・API未接続

呼出: `mpr` / [原本 L504](../../crawl-ref/source/wiz-dgn.cc#L504)

### 原文（静的キー）

```text
NOTE: Shaft traps aren't valid on this level.
```

### 日本語

未訳（英語にフォールバック）。

## 17. L513 — 未訳・API未接続

呼出: `mpr` / [原本 L513](../../crawl-ref/source/wiz-dgn.cc#L513)

### 原文（静的キー）

```text
Insufficient floor-space for new Wal-Mart.
```

### 日本語

未訳（英語にフォールバック）。

## 18. L532 — 未訳・API未接続

呼出: `mpr` / [原本 L532](../../crawl-ref/source/wiz-dgn.cc#L532)

### 原文（静的キー）

```text
Done.
```

### 日本語

未訳（英語にフォールバック）。

## 19. L562 — 未訳・API未接続

呼出: `mprf` / [原本 L562](../../crawl-ref/source/wiz-dgn.cc#L562)

### 原文（静的キー）

```text
Can't find map named '%s'.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L601 — 未訳・API未接続

呼出: `mprf` / [原本 L601](../../crawl-ref/source/wiz-dgn.cc#L601)

### 原文（静的キー）

```text
Placing %s on top of you would put part of the map outside of the level, cancelling.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 21. L612 — 未訳・API未接続

呼出: `mprf` / [原本 L612](../../crawl-ref/source/wiz-dgn.cc#L612)

### 原文（静的キー）

```text
%s decides where it goes, can't place where you are.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 22. L626 — 未訳・API未接続

呼出: `mpr` / [原本 L626](../../crawl-ref/source/wiz-dgn.cc#L626)

### 原文（静的キー）

```text
Ok; try placing with &L or go to the relevant location to safely place with &P.
```

### 日本語

未訳（英語にフォールバック）。

## 23. L653 — 未訳・API未接続

呼出: `mprf` / [原本 L653](../../crawl-ref/source/wiz-dgn.cc#L653)

### 原文（静的キー）

```text
Successfully placed %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L663 — 未訳・API未接続

呼出: `mprf` / [原本 L663](../../crawl-ref/source/wiz-dgn.cc#L663)

### 原文（静的キー）

```text
Failed to place %s; last builder error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L675 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L675](../../crawl-ref/source/wiz-dgn.cc#L675)

### 未解決の式（断片を翻訳キーにしない）

```text
primary ? "Enter map name: " :
         "Enter map name (prefix it with * for local placement): "
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L767 — 未訳・API未接続

呼出: `mprf` / [原本 L767](../../crawl-ref/source/wiz-dgn.cc#L767)

### 原文（静的キー）

```text
Explore took %d turns.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 27. L774 — 未訳・API未接続

呼出: `mpr` / [原本 L774](../../crawl-ref/source/wiz-dgn.cc#L774)

### 原文（静的キー）

```text
Level stack:
```

### 日本語

未訳（英語にフォールバック）。

## 28. L777 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L777](../../crawl-ref/source/wiz-dgn.cc#L777)

### 未解決の式（断片を翻訳キーにしない）

```text
i+1
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 29. L787 — 未訳・API未接続

呼出: `mpr` / [原本 L787](../../crawl-ref/source/wiz-dgn.cc#L787)

### 原文（静的キー）

```text
Known levels:
```

### 日本語

未訳（英語にフォールバック）。

## 30. L797 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L797](../../crawl-ref/source/wiz-dgn.cc#L797)

### 未解決の式（断片を翻訳キーにしない）

```text
i+1
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 31. L805 — 未訳・API未接続

呼出: `mprf` / [原本 L805](../../crawl-ref/source/wiz-dgn.cc#L805)

### 原文（静的キー）

```text
%-10s : %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 32. L810 — 未訳・API未接続

呼出: `mpr` / [原本 L810](../../crawl-ref/source/wiz-dgn.cc#L810)

### 原文（静的キー）

```text
Regenerating level.
```

### 日本語

未訳（英語にフォールバック）。

## 33. L868 — 未訳・API未接続

呼出: `mpr` / [原本 L868](../../crawl-ref/source/wiz-dgn.cc#L868)

### 原文（静的キー）

```text
All vaults are now eligible for [re]use.
```

### 日本語

未訳（英語にフォールバック）。

## 34. L874 — 未訳・API未接続

呼出: `mprf` / [原本 L874](../../crawl-ref/source/wiz-dgn.cc#L874)

### 原文（静的キー）

```json
"Set Abyss speed to what? (now %d, higher value = higher speed) "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 35. L882 — 未訳・API未接続

呼出: `mprf` / [原本 L882](../../crawl-ref/source/wiz-dgn.cc#L882)

### 原文（静的キー）

```text
Setting Abyss speed to %i.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
