# コードメッセージ: `terrain.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L940 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L940](../../crawl-ref/source/terrain.cc#L940)

### 未解決の式（断片を翻訳キーにしない）

```text
(walls > 1) ? "The walls %s %s!" : "The wall %ss %s!"
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L981 — 未訳・API未接続

呼出: `mprf` / [原本 L981](../../crawl-ref/source/terrain.cc#L981)

### 原文（静的キー）

```text
You hear a splash.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L985 — 未訳・API未接続

呼出: `mprf` / [原本 L985](../../crawl-ref/source/terrain.cc#L985)

### 原文（静的キー）

```text
You hear a sizzling splash.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L1482 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L1482](../../crawl-ref/source/terrain.cc#L1482)

### 未解決の式（断片を翻訳キーにしない）

```text
str.str()
```

## 5. L1576 — 未訳・API未接続

呼出: `mprf` / [原本 L1576](../../crawl-ref/source/terrain.cc#L1576)

### 原文（静的キー）

```text
swap_features(): No boring squares on level?
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L1791 — 未訳・API未接続

呼出: `mprf` / [原本 L1791](../../crawl-ref/source/terrain.cc#L1791)

### 原文（静的キー）

```text
You fall into the %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L2621 — 未訳・API未接続

呼出: `mprf` / [原本 L2621](../../crawl-ref/source/terrain.cc#L2621)

### 原文（静的キー）

```text
The wall freezes %s%s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L2664 — 未訳・API未接続

呼出: `mprf` / [原本 L2664](../../crawl-ref/source/terrain.cc#L2664)

### 原文（静的キー）

```text
The frigid air chills %s%s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L2707 — 未訳・API未接続

呼出: `mpr` / [原本 L2707](../../crawl-ref/source/terrain.cc#L2707)

### 原文（静的キー）

```text
The exit collapses.
```

### 日本語

未訳（英語にフォールバック）。
