# コードメッセージ: `arena.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L614 — 未訳・API未接続

呼出: `mprf` / [原本 L614](../../crawl-ref/source/arena.cc#L614)

### 原文（静的キー）

```text
Book-keeping says faction_a has negative active members.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L617 — 未訳・API未接続

呼出: `mprf` / [原本 L617](../../crawl-ref/source/arena.cc#L617)

### 原文（静的キー）

```text
Book-keeping says faction_b has negative active members.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L635 — 未訳・API未接続

呼出: `mprf` / [原本 L635](../../crawl-ref/source/arena.cc#L635)

### 原文（静的キー）

```text
Book-keeping error in faction member count: %d:%d instead of %d:%d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L662 — 未訳・API未接続

呼出: `mprf` / [原本 L662](../../crawl-ref/source/arena.cc#L662)

### 原文（静的キー）

```text
Both factions alive but one declared the winner.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L686 — 未訳・API未接続

呼出: `mprf` / [原本 L686](../../crawl-ref/source/arena.cc#L686)

### 原文（静的キー）

```text
ERROR: Both sides have spawners, but the active member count of one side has been reduced to zero!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L787 — 未訳・API未接続

呼出: `mprf` / [原本 L787](../../crawl-ref/source/arena.cc#L787)

### 原文（静的キー）

```text
Dismissing non-respawner %s to make room for respawner whose side has 0 active members.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L796 — 未訳・API未接続

呼出: `mprf` / [原本 L796](../../crawl-ref/source/arena.cc#L796)

### 原文（静的キー）

```text
Teleporting respawner %s to make room for other respawner whose side has 0 active members.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L837 — 未訳・API未接続

呼出: `mprf` / [原本 L837](../../crawl-ref/source/arena.cc#L837)

### 原文（静的キー）

```text
---- Turn #%d ----
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L868 — 未訳・API未接続

呼出: `mpr` / [原本 L868](../../crawl-ref/source/arena.cc#L868)

### 原文（静的キー）

```text
Cancelling contest at user request
```

### 日本語

未訳（英語にフォールバック）。

## 10. L885 — 未訳・API未接続

呼出: `mprf` / [原本 L885](../../crawl-ref/source/arena.cc#L885)

### 原文（静的キー）

```text
Tie declared, but faction_a won.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L891 — 未訳・API未接続

呼出: `mprf` / [原本 L891](../../crawl-ref/source/arena.cc#L891)

### 原文（静的キー）

```text
Tie declared, but faction_b won.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L905 — 未訳・API未接続

呼出: `mprf` / [原本 L905](../../crawl-ref/source/arena.cc#L905)

### 原文（静的キー）

```text
*BOTH* factions won?!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L908 — 未訳・API未接続

呼出: `mprf` / [原本 L908](../../crawl-ref/source/arena.cc#L908)

### 原文（静的キー）

```text
Faction_a real winner.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L914 — 未訳・API未接続

呼出: `mprf` / [原本 L914](../../crawl-ref/source/arena.cc#L914)

### 原文（静的キー）

```text
Faction_b real winner.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L919 — 未訳・API未接続

呼出: `mprf` / [原本 L919](../../crawl-ref/source/arena.cc#L919)

### 原文（静的キー）

```text
Both sides dead.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L939 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L939](../../crawl-ref/source/arena.cc#L939)

### 未解決の式（断片を翻訳キーにしない）

```text
msg
```

## 17. L941 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L941](../../crawl-ref/source/arena.cc#L941)

### 未解決の式（断片を翻訳キーにしない）

```text
msg.c_str()
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L1077 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L1077](../../crawl-ref/source/arena.cc#L1077)

### 未解決の式（断片を翻訳キーにしない）

```text
"---- Contest finished ----\n" + outcome
```

## 19. L1201 — 未訳・API未接続

呼出: `mprf` / [原本 L1201](../../crawl-ref/source/arena.cc#L1201)

### 原文（静的キー）

```text
%s %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L1278 — 未訳・API未接続

呼出: `mpr` / [原本 L1278](../../crawl-ref/source/arena.cc#L1278)

### 原文（静的キー）

```text
Last arena monster was dismissed.
```

### 日本語

未訳（英語にフォールバック）。
