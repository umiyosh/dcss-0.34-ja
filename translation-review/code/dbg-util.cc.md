# コードメッセージ: `dbg-util.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L32 — 未訳・API未接続

呼出: `mprf` / [原本 L32](../../crawl-ref/source/dbg-util.cc#L32)

### 原文（静的キー）

```json
"Which monster by name? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L83 — 未訳・API未接続

呼出: `mpr` / [原本 L83](../../crawl-ref/source/dbg-util.cc#L83)

### 原文（静的キー）

```text
Currently generating level.
```

### 日本語

未訳（英語にフォールバック）。

## 3. L89 — 未訳・API未接続

呼出: `mprf` / [原本 L89](../../crawl-ref/source/dbg-util.cc#L89)

### 原文（静的キー）

```text
Vault being placed: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L92 — 未訳・API未接続

呼出: `mprf` / [原本 L92](../../crawl-ref/source/dbg-util.cc#L92)

### 原文（静的キー）

```text
Subvaults: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L110 — 未訳・API未接続

呼出: `mprf` / [原本 L110](../../crawl-ref/source/dbg-util.cc#L110)

### 原文（静的キー）

```text
Level build method = %s, level layout type  = %s, absdepth0 = %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L115 — 未訳・API未接続

呼出: `mpr` / [原本 L115](../../crawl-ref/source/dbg-util.cc#L115)

### 原文（静的キー）

```text
Level vaults:
```

### 日本語

未訳（英語にフォールバック）。

## 7. L118 — 未訳・API未接続

呼出: `mprf` / [原本 L118](../../crawl-ref/source/dbg-util.cc#L118)

### 原文（静的キー）

```text
    %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L120 — 未訳・API未接続

呼出: `mpr` / [原本 L120](../../crawl-ref/source/dbg-util.cc#L120)

### 原文（静的キー）

```text

```

### 日本語

未訳（英語にフォールバック）。

## 9. L127 — 未訳・API未接続

呼出: `mpr` / [原本 L127](../../crawl-ref/source/dbg-util.cc#L127)

### 原文（静的キー）

```text
This save was not generated on a build that stores logs.
```

### 日本語

未訳（英語にフォールバック）。

## 10. L135 — 未訳・API未接続

呼出: `mprf` / [原本 L135](../../crawl-ref/source/dbg-util.cc#L135)

### 原文（静的キー）

```text
No builder logs are saved for %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L404 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L404](../../crawl-ref/source/dbg-util.cc#L404)

### 未解決の式（断片を翻訳キーにしない）

```text
chan
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L405 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L405](../../crawl-ref/source/dbg-util.cc#L405)

### 未解決の式（断片を翻訳キーにしない）

```text
chan
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L407 — 未訳・API未接続

呼出: `mprf` / [原本 L407](../../crawl-ref/source/dbg-util.cc#L407)

### 原文（静的キー）

```text
    item #%d:  base: %d; sub: %d; plus: %d; plus2: %d; special: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L411 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L411](../../crawl-ref/source/dbg-util.cc#L411)

### 未解決の式（断片を翻訳キーにしない）

```text
"    quant: %d; ident: 0x%08" PRIx32"; ident_type: %d"
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L414 — 未訳・API未接続

呼出: `mprf` / [原本 L414](../../crawl-ref/source/dbg-util.cc#L414)

### 原文（静的キー）

```text
    x: %d; y: %d; link: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L502 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L502](../../crawl-ref/source/dbg-util.cc#L502)

### 未解決の式（断片を翻訳キーにしない）

```text
message
```

## 17. L528 — 未訳・API未接続

呼出: `mpr` / [原本 L528](../../crawl-ref/source/dbg-util.cc#L528)

### 原文（静的キー）

```text
Diagnostic messages are available only in debug builds.
```

### 日本語

未訳（英語にフォールバック）。

## 18. L566 — 未訳・API未接続

呼出: `mprf` / [原本 L566](../../crawl-ref/source/dbg-util.cc#L566)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L570 — 未訳・API未接続

呼出: `mprf` / [原本 L570](../../crawl-ref/source/dbg-util.cc#L570)

### 原文（静的キー）

```json
"Toggle which debug class (ESC to exit)? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L586 — 未訳・API未接続

呼出: `mprf` / [原本 L586](../../crawl-ref/source/dbg-util.cc#L586)

### 原文（静的キー）

```text
%s messages will be %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
