# コードメッセージ: `dungeon.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L343 — 未訳・API未接続

呼出: `mprf` / [原本 L343](../../crawl-ref/source/dungeon.cc#L343)

### 原文（静的キー）

```text
Builder paused after veto; use &ctrl-r to resume.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L392 — 未訳・API未接続

呼出: `mprf` / [原本 L392](../../crawl-ref/source/dungeon.cc#L392)

### 原文（静的キー）

```text
Failed to load map, reloading all maps (%s).
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L406 — 未訳・API未接続

呼出: `mprf` / [原本 L406](../../crawl-ref/source/dungeon.cc#L406)

### 原文（静的キー）

```text
Aborting &P builder on fatal lua error
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L411 — 未訳・API未接続

呼出: `mprf` / [原本 L411](../../crawl-ref/source/dungeon.cc#L411)

### 原文（静的キー）

```text
Builder <white>VETO</white> on fatal lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L433 — 未訳・API未接続

呼出: `mprf` / [原本 L433](../../crawl-ref/source/dungeon.cc#L433)

### 原文（静的キー）

```text
Unable to generate level for '%s'!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L530 — 未訳・API未接続

呼出: `mprf` / [原本 L530](../../crawl-ref/source/dungeon.cc#L530)

### 原文（静的キー）

```text
branch epilogue for %s failed: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L601 — 未訳・API未接続

呼出: `mprf` / [原本 L601](../../crawl-ref/source/dungeon.cc#L601)

### 原文（静的キー）

```text
Multiple locations with transporter destination name %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L618 — 未訳・API未接続

呼出: `mprf` / [原本 L618](../../crawl-ref/source/dungeon.cc#L618)

### 原文（静的キー）

```text
Transporter with name %s has no corresponding destination marker.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L636 — 未訳・API未接続

呼出: `mprf` / [原本 L636](../../crawl-ref/source/dungeon.cc#L636)

### 原文（静的キー）

```text
Unused transporter destination with name %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L1746 — 未訳・API未接続

呼出: `mprf` / [原本 L1746](../../crawl-ref/source/dungeon.cc#L1746)

### 原文（静的キー）

```text
Item %s buggily placed in feature %s at (%d, %d).
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L1753 — 未訳・API未接続

呼出: `mprf` / [原本 L1753](../../crawl-ref/source/dungeon.cc#L1753)

### 原文（静的キー）

```text
Item buggily placed out of bounds at (%d, %d).
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L1820 — 未訳・API未接続

呼出: `mprf` / [原本 L1820](../../crawl-ref/source/dungeon.cc#L1820)

### 原文（静的キー）

```text
Multiple branch exits on %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L2409 — 未訳・API未接続

呼出: `mprf` / [原本 L2409](../../crawl-ref/source/dungeon.cc#L2409)

### 原文（静的キー）

```text
Couldn't find overflow temple map '%s'!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L2442 — 未訳・API未接続

呼出: `mprf` / [原本 L2442](../../crawl-ref/source/dungeon.cc#L2442)

### 原文（静的キー）

```text
Already placed specialized single-altar temple for %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L2452 — 未訳・API未接続

呼出: `mprf` / [原本 L2452](../../crawl-ref/source/dungeon.cc#L2452)

### 原文（静的キー）

```text
Couldn't find overflow temple for combination of tags %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L2466 — 未訳・API未接続

呼出: `mprf` / [原本 L2466](../../crawl-ref/source/dungeon.cc#L2466)

### 原文（静的キー）

```text
Couldn't find overflow temple tag '%s'!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L2485 — 未訳・API未接続

呼出: `mprf` / [原本 L2485](../../crawl-ref/source/dungeon.cc#L2485)

### 原文（静的キー）

```text
Couldn't place overflow temple '%s', vetoing level.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L2492 — 未訳・API未接続

呼出: `mprf` / [原本 L2492](../../crawl-ref/source/dungeon.cc#L2492)

### 原文（静的キー）

```text
Placed overflow temple %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L3051 — 未訳・API未接続

呼出: `mprf` / [原本 L3051](../../crawl-ref/source/dungeon.cc#L3051)

### 原文（静的キー）

```text
Overriding seed-determined temple map.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L3061 — 未訳・API未接続

呼出: `mprf` / [原本 L3061](../../crawl-ref/source/dungeon.cc#L3061)

### 原文（静的キー）

```text
Unable to find Temple vault '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 21. L3438 — 未訳・API未接続

呼出: `mpr` / [原本 L3438](../../crawl-ref/source/dungeon.cc#L3438)

### 原文（静的キー）

```text
Whoops, nicked a vault in slime connectivity fixup
```

### 日本語

未訳（英語にフォールバック）。

## 22. L4420 — 未訳・API未接続

呼出: `mprf` / [原本 L4420](../../crawl-ref/source/dungeon.cc#L4420)

### 原文（静的キー）

```text
Unable to find exit from %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 23. L4481 — 未訳・API未接続

呼出: `mprf` / [原本 L4481](../../crawl-ref/source/dungeon.cc#L4481)

### 原文（静的キー）

```text
Cannot generate encompass map '%s' with check_collision=true
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L4554 — 未訳・API未接続

呼出: `mprf` / [原本 L4554](../../crawl-ref/source/dungeon.cc#L4554)

### 原文（静的キー）

```text
Failed to load map %s in dgn_safe_place_map, reloading all maps
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L5750 — 未訳・API未接続

呼出: `mprf` / [原本 L5750](../../crawl-ref/source/dungeon.cc#L5750)

### 原文（静的キー）

```text
ERROR: %s already generated somewhere else; please file a bug report.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L6045 — 未訳・API未接続

呼出: `mprf` / [原本 L6045](../../crawl-ref/source/dungeon.cc#L6045)

### 原文（静的キー）

```text
Ran out of altars for temple!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 27. L6607 — 未訳・API未接続

呼出: `mprf` / [原本 L6607](../../crawl-ref/source/dungeon.cc#L6607)

### 原文（静的キー）

```text
%s%s tried to place a shaft at a branch end.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
