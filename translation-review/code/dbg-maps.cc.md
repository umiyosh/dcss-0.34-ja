# コードメッセージ: `dbg-maps.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L81 — 未訳・API未接続

呼出: `mprf` / [原本 L81](../../crawl-ref/source/dbg-maps.cc#L81)

### 原文（静的キー）

```text
On %s; %d g, %d fail, %u err%s, %u uniq, %d try, %d (%.2f%%) vetos
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L94 — 未訳・API未接続

呼出: `mprf` / [原本 L94](../../crawl-ref/source/dbg-maps.cc#L94)

### 原文（静的キー）

```text
User requested cancel
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L174 — 未訳・API未接続

呼出: `mprf` / [原本 L174](../../crawl-ref/source/dbg-maps.cc#L174)

### 原文（静的キー）

```text
Bad (disconnected) level on %s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L246 — 未訳・API未接続

呼出: `mprf` / [原本 L246](../../crawl-ref/source/dbg-maps.cc#L246)

### 原文（静的キー）

```text
On %d of %d; %d g, %d fail, %u err%s, %u uniq, %d try, %d (%.2f%%) vetoes
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L328 — 未訳・API未接続

呼出: `mprf` / [原本 L328](../../crawl-ref/source/dbg-maps.cc#L328)

### 原文（静的キー）

```text
Examining random maps at %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L541 — 未訳・API未接続

呼出: `mpr` / [原本 L541](../../crawl-ref/source/dbg-maps.cc#L541)

### 原文（静的キー）

```text
Generating dungeon map stats
```

### 日本語

未訳（英語にフォールバック）。
