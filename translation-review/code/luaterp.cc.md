# コードメッセージ: `luaterp.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L102 — 未訳・API未接続

呼出: `mprf` / [原本 L102](../../crawl-ref/source/luaterp.cc#L102)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L115 — 未訳・API未接続

呼出: `mpr` / [原本 L115](../../crawl-ref/source/luaterp.cc#L115)

### 原文（静的キー）

```text
[Hit ESC to exit interpreter.]
```

### 日本語

未訳（英語にフォールバック）。

## 3. L127 — 未訳・API未接続

呼出: `mprf` / [原本 L127](../../crawl-ref/source/luaterp.cc#L127)

### 原文（静的キー）

```text
error calling __echo (%s)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L153 — 未訳・API未接続

呼出: `mprf` / [原本 L153](../../crawl-ref/source/luaterp.cc#L153)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
