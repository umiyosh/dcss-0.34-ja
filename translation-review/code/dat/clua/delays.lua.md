# コードメッセージ: `dat/clua/delays.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L107 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L107](../../../../crawl-ref/source/dat/clua/delays.lua#L107)

### 未解決の式（断片を翻訳キーにしない）

```text
"Missing channel name for runrest: '" .. s .. "'"
```

## 2. L110 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L110](../../../../crawl-ref/source/dat/clua/delays.lua#L110)

### 未解決の式（断片を翻訳キーにしない）

```text
"Bad channel name '" .. chstr .. "' for runrest: '" .. s .. "'"
```
