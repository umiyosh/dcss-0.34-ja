# コードメッセージ: `dat/clua/autofight.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L193 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L193](../../../../crawl-ref/source/dat/clua/autofight.lua#L193)

### 原文（静的キー）

```text
Failed to move towards target.
```

### 日本語

未訳（英語にフォールバック）。

## 2. L198 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L198](../../../../crawl-ref/source/dat/clua/autofight.lua#L198)

### 原文（静的キー）

```text
Failed to move towards target because you cannot move.
```

### 日本語

未訳（英語にフォールバック）。

## 3. L417 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L417](../../../../crawl-ref/source/dat/clua/autofight.lua#L417)

### 原文（静的キー）

```text
You are too injured to fight recklessly!
```

### 日本語

未訳（英語にフォールバック）。

## 4. L420 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L420](../../../../crawl-ref/source/dat/clua/autofight.lua#L420)

### 原文（静的キー）

```text
You are too confused!
```

### 日本語

未訳（英語にフォールバック）。

## 5. L423 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L423](../../../../crawl-ref/source/dat/clua/autofight.lua#L423)

### 未解決の式（断片を翻訳キーにしない）

```text
"You are " .. caught .. "!"
```

## 6. L444 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L444](../../../../crawl-ref/source/dat/clua/autofight.lua#L444)

### 原文（静的キー）

```text
No target in view!
```

### 日本語

未訳（英語にフォールバック）。

## 7. L453 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L453](../../../../crawl-ref/source/dat/clua/autofight.lua#L453)

### 原文（静的キー）

```text
No reachable target in view!
```

### 日本語

未訳（英語にフォールバック）。

## 8. L461 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L461](../../../../crawl-ref/source/dat/clua/autofight.lua#L461)

### 原文（静的キー）

```text
No target in range!
```

### 日本語

未訳（英語にフォールバック）。

## 9. L504 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L504](../../../../crawl-ref/source/dat/clua/autofight.lua#L504)

### 未解決の式（断片を翻訳キーにしない）

```text
"No spell in slot " .. AUTOMAGIC_SPELL_SLOT .. "!"
```

## 10. L512 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L512](../../../../crawl-ref/source/dat/clua/autofight.lua#L512)

### 未解決の式（断片を翻訳キーにしない）

```text
"No spell in slot " .. AUTOMAGIC_SPELL_SLOT .. "!"
```

## 11. L518 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L518](../../../../crawl-ref/source/dat/clua/autofight.lua#L518)

### 未解決の式（断片を翻訳キーにしない）

```text
AUTOFIGHT_THROW and "Enabling autothrow." or "Disabling autothrow."
```
