# コードメッセージ: `dat/clua/automagic.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L107 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L107](../../../../crawl-ref/source/dat/clua/automagic.lua#L107)

### 原文（静的キー）

```text
Failed to move towards target.
```

### 日本語

未訳（英語にフォールバック）。

## 2. L252 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L252](../../../../crawl-ref/source/dat/clua/automagic.lua#L252)

### 原文（静的キー）

```text
You are too injured to fight recklessly!
```

### 日本語

未訳（英語にフォールバック）。

## 3. L254 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L254](../../../../crawl-ref/source/dat/clua/automagic.lua#L254)

### 原文（静的キー）

```text
You are too confused!
```

### 日本語

未訳（英語にフォールバック）。

## 4. L256 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L256](../../../../crawl-ref/source/dat/clua/automagic.lua#L256)

### 原文（静的キー）

```text
No target in view!
```

### 日本語

未訳（英語にフォールバック）。

## 5. L263 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L263](../../../../crawl-ref/source/dat/clua/automagic.lua#L263)

### 未解決の式（断片を翻訳キーにしない）

```text
"You don't have enough magic to cast " ..
          you.spell_table()[AUTOMAGIC_SPELL_SLOT] .. "!"
```

## 6. L270 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L270](../../../../crawl-ref/source/dat/clua/automagic.lua#L270)

### 原文（静的キー）

```text
You are too depleted to cast spells recklessly!
```

### 日本語

未訳（英語にフォールバック）。

## 7. L277 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L277](../../../../crawl-ref/source/dat/clua/automagic.lua#L277)

### 原文（静的キー）

```text
No target in range!
```

### 日本語

未訳（英語にフォールバック）。

## 8. L283 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L283](../../../../crawl-ref/source/dat/clua/automagic.lua#L283)

### 原文（静的キー）

```text
Which spell slot to assign to automagic? (Enter to disable, Esc to cancel)
```

### 日本語

未訳（英語にフォールバック）。

## 9. L288 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L288](../../../../crawl-ref/source/dat/clua/automagic.lua#L288)

### 原文（静的キー）

```text
Cancelled.
```

### 日本語

未訳（英語にフォールバック）。

## 10. L292 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L292](../../../../crawl-ref/source/dat/clua/automagic.lua#L292)

### 原文（静的キー）

```text
Deactivated automagic.
```

### 日本語

未訳（英語にフォールバック）。

## 11. L297 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L297](../../../../crawl-ref/source/dat/clua/automagic.lua#L297)

### 原文（静的キー）

```text
Invalid spell slot.
```

### 日本語

未訳（英語にフォールバック）。

## 12. L310 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L310](../../../../crawl-ref/source/dat/clua/automagic.lua#L310)

### 未解決の式（断片を翻訳キーにしない）

```text
"Automagic" .. message .. " will cast spell in slot " .. slot .. " (" ..
        spell_name .. ")" .. "."
```
