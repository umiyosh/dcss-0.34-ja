# コードメッセージ: `dat/dlua/layout/geoelf_rooms.lua`

[コードメッセージ一覧](../../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L274 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L274](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L274)

### 原文（静的キー）

```text
Error: No room data array
```

### 日本語

未訳（英語にフォールバック）。

## 2. L277 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L277](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L277)

### 原文（静的キー）

```text
Error: Room data array does not have count
```

### 日本語

未訳（英語にフォールバック）。

## 3. L280 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L280](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L280)

### 原文（静的キー）

```text
Error: index is past the end of room_data
```

### 日本語

未訳（英語にフォールバック）。

## 4. L283 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L283](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L283)

### 原文（静的キー）

```text
Error: Room index does not exist
```

### 日本語

未訳（英語にフォールバック）。

## 5. L323 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L323](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L323)

### 未解決の式（断片を翻訳キーにしない）

```text
"Error: Trying to draw room of invalid shape " .. shape
```

## 6. L339 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L339](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L339)

### 未解決の式（断片を翻訳キーにしない）

```text
"Error: room_value is nil at " .. index_x ..
                          ", " .. index_y
```

## 7. L376 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L376](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L376)

### 未解決の式（断片を翻訳キーにしない）

```text
"Error: Existing room glyph is nil at " .. x .. ", " .. y
```

## 8. L403 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L403](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L403)

### 未解決の式（断片を翻訳キーにしない）

```text
"Error: New room glyph is nil at " .. x .. ", " .. y ..
                    " room_value = " .. room_value .. ", old_glyph = " ..
                    old_glyph
```

## 9. L415 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L415](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L415)

### 原文（静的キー）

```text
Error: No room data array
```

### 日本語

未訳（英語にフォールバック）。

## 10. L418 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L418](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L418)

### 原文（静的キー）

```text
Error: Room data array does not have count
```

### 日本語

未訳（英語にフォールバック）。

## 11. L421 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L421](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L421)

### 原文（静的キー）

```text
Error: index is past the end of room_data
```

### 日本語

未訳（英語にフォールバック）。

## 12. L424 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L424](../../../../../crawl-ref/source/dat/dlua/layout/geoelf_rooms.lua#L424)

### 原文（静的キー）

```text
Error: Room index does not exist
```

### 日本語

未訳（英語にフォールバック）。
