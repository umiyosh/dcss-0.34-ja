# コードメッセージ: `dat/dlua/util.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L505 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L505](../../../../crawl-ref/source/dat/dlua/util.lua#L505)

### 未解決の式（断片を翻訳キーにしない）

```text
what .. ": " .. (now - last) .. " ms"
```
