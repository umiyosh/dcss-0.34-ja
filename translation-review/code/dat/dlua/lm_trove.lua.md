# コードメッセージ: `dat/dlua/lm_trove.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L160 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L160](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L160)

### 原文（静的キー）

```text
Switched to a new item.
```

### 日本語

未訳（英語にフォールバック）。

## 2. L162 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L162](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L162)

### 未解決の式（断片を翻訳キーにしない）

```text
"Ignoring " .. new_item.name()
```

## 3. L165 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L165](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L165)

### 未解決の式（断片を翻訳キーにしない）

```text
"To change the item the trove wants, first drop it "
                .. "on the trove tile."
```

## 4. L179 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L179](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L179)

### 原文（静的キー）

```text
It's a nopiety trove.
```

### 日本語

未訳（英語にフォールバック）。

## 5. L206 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L206](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L206)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted quantity: " .. item.quantity
```

## 6. L208 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L208](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L208)

### 原文（静的キー）

```text
Wanted quantity: nil.
```

### 日本語

未訳（英語にフォールバック）。

## 7. L212 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L212](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L212)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted base type: " .. item.base_type
```

## 8. L214 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L214](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L214)

### 原文（静的キー）

```text
Wanted base type: nil
```

### 日本語

未訳（英語にフォールバック）。

## 9. L218 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L218](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L218)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted sub type: " .. item.sub_type
```

## 10. L220 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L220](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L220)

### 原文（静的キー）

```text
Wanted sub type: nil
```

### 日本語

未訳（英語にフォールバック）。

## 11. L224 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L224](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L224)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted ego type: " .. item.ego_type
```

## 12. L226 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L226](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L226)

### 原文（静的キー）

```text
Wanted ego type: nil
```

### 日本語

未訳（英語にフォールバック）。

## 13. L230 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L230](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L230)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted plus1: " .. item.plus1
```

## 14. L232 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L232](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L232)

### 原文（静的キー）

```text
Unwanted plus1.
```

### 日本語

未訳（英語にフォールバック）。

## 15. L236 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L236](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L236)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted artefact name: " .. item.artefact_name
```

## 16. L238 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L238](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L238)

### 原文（静的キー）

```text
Unwanted artefact.
```

### 日本語

未訳（英語にフォールバック）。

## 17. L372 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L372](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L372)

### 未解決の式（断片を翻訳キーにしない）

```text
"Accepting " .. item.ego_type .. "."
```

## 18. L375 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L375](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L375)

### 未解決の式（断片を翻訳キーにしない）

```text
"No " .. item.ego_type .. "."
```

## 19. L394 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L394](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L394)

### 原文（静的キー）

```text
No items.
```

### 日本語

未訳（英語にフォールバック）。

## 20. L412 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L412](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L412)

### 未解決の式（断片を翻訳キーにしない）

```text
"Checking item: " .. it.name()
```

## 21. L415 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L415](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L415)

### 原文（静的キー）

```text
Item not identified.
```

### 日本語

未訳（英語にフォールバック）。

## 22. L435 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L435](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L435)

### 原文（静的キー）

```text
Nil plus1 when we want plus1.
```

### 日本語

未訳（英語にフォールバック）。

## 23. L443 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L443](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L443)

### 未解決の式（断片を翻訳キーにしない）

```text
"Pluses do not match: " .. item.plus1 .. " versus " .. iplus1
```

## 24. L448 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L448](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L448)

### 原文（静的キー）

```text
Quantity is nil.
```

### 日本語

未訳（英語にフォールバック）。

## 25. L451 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L451](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L451)

### 未解決の式（断片を翻訳キーにしない）

```text
"Quantity does not match, want " .. item.quantity .. ", got " .. it.quantity
```

## 26. L457 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L457](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L457)

### 未解決の式（断片を翻訳キーにしない）

```text
"Artefact name does not match, want " .. item.artefact_name .. ", got " .. it.artefact_name
```

## 27. L464 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L464](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L464)

### 未解決の式（断片を翻訳キーにしない）

```text
"Ego type does not match, want " .. item.ego_type .. ", got " .. it.ego_type
```

## 28. L473 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L473](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L473)

### 未解決の式（断片を翻訳キーにしない）

```text
"Accepting " .. it.name() .. " and added to queue."
```

## 29. L476 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L476](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L476)

### 原文（静的キー）

```text
Sub and base types do not match.
```

### 日本語

未訳（英語にフォールバック）。

## 30. L477 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L477](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L477)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted base type " .. item.base_type .. ", got " .. it.base_type
```

## 31. L478 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L478](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L478)

### 未解決の式（断片を翻訳キーにしない）

```text
"Wanted sub type " .. item.sub_type .. ", got " .. it.sub_type
```

## 32. L536 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L536](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L536)

### 原文（静的キー）

```text
Okay, then.
```

### 日本語

未訳（英語にフォールバック）。

## 33. L543 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L543](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L543)

### 原文（静的キー）

```text
Okay, then.
```

### 日本語

未訳（英語にフォールバック）。

## 34. L551 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L551](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L551)

### 未解決の式（断片を翻訳キーにしない）

```text
"The portal draws power from the presence of the item"
                .. self:plural() .. " and buzzes to life!"
```

## 35. L556 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L556](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L556)

### 未解決の式（断片を翻訳キーにしない）

```text
"You don't have " .. self:item_name() .. " with you."
```

## 36. L567 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L567](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L567)

### 未解決の式（断片を翻訳キーにしない）

```text
"You don't have " .. self:item_name() .. " with you."
```

## 37. L569 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L569](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L569)

### 未解決の式（断片を翻訳キーにしない）

```text
"You don't have the item" .. self:plural() .. " to give!"
```

## 38. L588 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L588](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L588)

### 未解決の式（断片を翻訳キーにしない）

```text
"The portal draws power from the presence of the item" ..
              self:plural() .. " and buzzes to life!"
```

## 39. L595 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L595](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L595)

### 未解決の式（断片を翻訳キーにしない）

```text
"You must unequip the item" .. self:plural() .. " first!"
```

## 40. L599 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L599](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L599)

### 未解決の式（断片を翻訳キーにしない）

```text
"The portal accepts the item" .. self:plural() ..
              " and buzzes to life!"
```

## 41. L650 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L650](../../../../crawl-ref/source/dat/dlua/lm_trove.lua#L650)

### 原文（静的キー）

```text
This trove is too buggy to enter!
```

### 日本語

未訳（英語にフォールバック）。
