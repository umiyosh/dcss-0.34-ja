# コードメッセージ: `dat/dlua/lm_fog.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L360 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L360](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L360)

### 未解決の式（断片を翻訳キーにしない）

```text
self.see_message
```

## 2. L362 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L362](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L362)

### 未解決の式（断片を翻訳キーにしない）

```text
self.cantsee_message
```

## 3. L394 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L394](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L394)

### 未解決の式（断片を翻訳キーにしない）

```text
self.see_message
```

## 4. L396 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L396](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L396)

### 未解決の式（断片を翻訳キーにしない）

```text
self.cantsee_message
```

## 5. L436 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L436](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L436)

### 未解決の式（断片を翻訳キーにしない）

```text
self.warning_see_message
```

## 6. L438 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L438](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L438)

### 未解決の式（断片を翻訳キーにしない）

```text
self.warning_cantsee_message
```

## 7. L446 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L446](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L446)

### 未解決の式（断片を翻訳キーにしない）

```text
self.trigger_see_message
```

## 8. L448 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L448](../../../../crawl-ref/source/dat/dlua/lm_fog.lua#L448)

### 未解決の式（断片を翻訳キーにしない）

```text
self.trigger_cantsee_message
```
