# コードメッセージ: `dat/dlua/lm_trig.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L158 — 未訳・API未接続

呼出: `crawl.mpr` / [原本 L158](../../../../crawl-ref/source/dat/dlua/lm_trig.lua#L158)

### 原文（静的キー）

```text
ERROR: Triggerable already removed
```

### 日本語

未訳（英語にフォールバック）。

## 2. L508 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L508](../../../../crawl-ref/source/dat/dlua/lm_trig.lua#L508)

### 未解決の式（断片を翻訳キーにしない）

```text
self.msg
```
