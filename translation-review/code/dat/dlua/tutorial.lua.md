# コードメッセージ: `dat/dlua/tutorial.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L10 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L10](../../../../crawl-ref/source/dat/dlua/tutorial.lua#L10)

### 未解決の式（断片を翻訳キーにしない）

```text
data.text
```

## 2. L30 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L30](../../../../crawl-ref/source/dat/dlua/tutorial.lua#L30)

### 未解決の式（断片を翻訳キーにしない）

```text
msg
```
