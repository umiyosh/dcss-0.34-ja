# コードメッセージ: `dat/dlua/lm_monst.lua`

[コードメッセージ一覧](../../README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L101 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L101](../../../../crawl-ref/source/dat/dlua/lm_monst.lua#L101)

### 未解決の式（断片を翻訳キーにしない）

```text
self.message_seen
```

## 2. L103 — 動的・未対応式（要調査・要改修）

呼出: `crawl.mpr` / [原本 L103](../../../../crawl-ref/source/dat/dlua/lm_monst.lua#L103)

### 未解決の式（断片を翻訳キーにしない）

```text
self.message_unseen
```
