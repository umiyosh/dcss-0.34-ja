# コードメッセージ: `files.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L533 — 未訳・API未接続

呼出: `mprf` / [原本 L533](../../crawl-ref/source/files.cc#L533)

### 原文（静的キー）

```text
Incomplete or corrupted data directory '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L541 — 未訳・API未接続

呼出: `mprf` / [原本 L541](../../crawl-ref/source/files.cc#L541)

### 原文（静的キー）

```text
Data directory '%s' found.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L1297 — 未訳・API未接続

呼出: `mprf` / [原本 L1297](../../crawl-ref/source/files.cc#L1297)

### 原文（静的キー）

```text
Your summoned %s left behind.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L1304 — 未訳・API未接続

呼出: `mprf` / [原本 L1304](../../crawl-ref/source/files.cc#L1304)

### 原文（静的キー）

```text
Your mindless puppet%s behind%s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L1402 — 未訳・API未接続

呼出: `mprf` / [原本 L1402](../../crawl-ref/source/files.cc#L1402)

### 原文（静的キー）

```text
Error: you smelly wizard, how dare you enter the same level (%s) twice! It will be trampled upon return.
The stack has: %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L1654 — 未訳・API未接続

呼出: `mprf` / [原本 L1654](../../crawl-ref/source/files.cc#L1654)

### 原文（静的キー）

```text
Second portal entrance for %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L1699 — 未訳・API未接続

呼出: `mprf` / [原本 L1699](../../crawl-ref/source/files.cc#L1699)

### 原文（静的キー）

```text
Portal %s already exists!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L1997 — 未訳・API未接続

呼出: `mprf` / [原本 L1997](../../crawl-ref/source/files.cc#L1997)

### 原文（静的キー）

```text
Emergency fixup: removing player from wall at %d,%d. Please report this as a bug!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L2119 — 未訳・API未接続

呼出: `mprf` / [原本 L2119](../../crawl-ref/source/files.cc#L2119)

### 原文（静的キー）

```text
Loading... branch: %d, level: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L2195 — 未訳・API未接続

呼出: `mprf` / [原本 L2195](../../crawl-ref/source/files.cc#L2195)

### 原文（静的キー）

```text
&P with '%s' failed; clearing force props and trying with random generation next.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L2345 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L2345](../../crawl-ref/source/files.cc#L2345)

### 未解決の式（断片を翻訳キーにしない）

```text
YELLOW
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L2371 — 未訳・API未接続

呼出: `mprf` / [原本 L2371](../../crawl-ref/source/files.cc#L2371)

### 原文（静的キー）

```text
global_info:: num_visits: %d, levels_seen: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L2379 — 未訳・API未接続

呼出: `mprf` / [原本 L2379](../../crawl-ref/source/files.cc#L2379)

### 原文（静的キー）

```text
curr_PlaceInfo:: num_visits: %d, levels_seen: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L2404 — 未訳・API未接続

呼出: `mprf` / [原本 L2404](../../crawl-ref/source/files.cc#L2404)

### 原文（静的キー）

```text
Fixing up corrupted PlaceInfo for %s (levels_seen is %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L2467 — 未訳・API未接続

呼出: `mprf` / [原本 L2467](../../crawl-ref/source/files.cc#L2467)

### 原文（静的キー）

```text
%s slides away from you right after you %s it!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L2474 — 未訳・API未接続

呼出: `mprf` / [原本 L2474](../../crawl-ref/source/files.cc#L2474)

### 原文（静的キー）

```text
%s settles down.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L2641 — 未訳・API未接続

呼出: `mpr` / [原本 L2641](../../crawl-ref/source/files.cc#L2641)

### 原文（静的キー）

```text
Char dump unsuccessful! Sorry about that.
```

### 日本語

未訳（英語にフォールバック）。

## 18. L2737 — 未訳・API未接続

呼出: `mprf` / [原本 L2737](../../crawl-ref/source/files.cc#L2737)

### 原文（静的キー）

```text
Bones file exists but can't be opened: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L2744 — 未訳・API未接続

呼出: `mprf` / [原本 L2744](../../crawl-ref/source/files.cc#L2744)

### 原文（静的キー）

```text
Unable to open bones file %s for writing
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L2762 — 未訳・API未接続

呼出: `mprf` / [原本 L2762](../../crawl-ref/source/files.cc#L2762)

### 原文（静的キー）

```text
Error installing bones file to %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 21. L2766 — 未訳・API未接続

呼出: `mprf` / [原本 L2766](../../crawl-ref/source/files.cc#L2766)

### 原文（静的キー）

```text
Failed to unlink probably corrupt bones file: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 22. L2862 — 未訳・API未接続

呼出: `mprf` / [原本 L2862](../../crawl-ref/source/files.cc#L2862)

### 原文（静的キー）

```text
Bones file to back up doesn't exist: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 23. L2869 — 未訳・API未接続

呼出: `mprf` / [原本 L2869](../../crawl-ref/source/files.cc#L2869)

### 原文（静的キー）

```text
Unable to open bones backup file %s for writing
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L2885 — 未訳・API未接続

呼出: `mprf` / [原本 L2885](../../crawl-ref/source/files.cc#L2885)

### 原文（静的キー）

```text
Error backing up bones file to %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L2889 — 未訳・API未接続

呼出: `mprf` / [原本 L2889](../../crawl-ref/source/files.cc#L2889)

### 原文（静的キー）

```text
Failed to unlink probably corrupt bones file: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L2929 — 未訳・API未接続

呼出: `mprf` / [原本 L2929](../../crawl-ref/source/files.cc#L2929)

### 原文（静的キー）

```text
Ghost file "%s" seems to be invalid (short read); deleting it.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 27. L3010 — 未訳・API未接続

呼出: `mprf` / [原本 L3010](../../crawl-ref/source/files.cc#L3010)

### 原文（静的キー）

```text
Mismatch between bones backup filename '%s' and version %d.%d!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 28. L3016 — 未訳・API未接続

呼出: `mprf` / [原本 L3016](../../crawl-ref/source/files.cc#L3016)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 29. L3024 — 未訳・API未接続

呼出: `mprf` / [原本 L3024](../../crawl-ref/source/files.cc#L3024)

### 原文（静的キー）

```text
%s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 30. L3045 — 未訳・API未接続

呼出: `mprf` / [原本 L3045](../../crawl-ref/source/files.cc#L3045)

### 原文（静的キー）

```text
Failed to unlink bones file: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 31. L3087 — 未訳・API未接続

呼出: `mprf` / [原本 L3087](../../crawl-ref/source/files.cc#L3087)

### 原文（静的キー）

```text
Placed ghost is not alive.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 32. L3090 — 未訳・API未接続

呼出: `mprf` / [原本 L3090](../../crawl-ref/source/files.cc#L3090)

### 原文（静的キー）

```text
Placed ghost is not MONS_PLAYER_GHOST, but %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 33. L3965 — 未訳・API未接続

呼出: `mprf` / [原本 L3965](../../crawl-ref/source/files.cc#L3965)

### 原文（静的キー）

```text
ERROR: Could not lock file %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 34. L3989 — 未訳・API未接続

呼出: `mprf` / [原本 L3989](../../crawl-ref/source/files.cc#L3989)

### 原文（静的キー）

```text
ERROR: Could not lock file %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
