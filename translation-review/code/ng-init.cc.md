# コードメッセージ: `ng-init.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L157 — 未訳・API未接続

呼出: `mprf` / [原本 L157](../../crawl-ref/source/ng-init.cc#L157)

### 原文（静的キー）

```text
Chose main temple %s, size %u
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L176 — 未訳・API未接続

呼出: `mprf` / [原本 L176](../../crawl-ref/source/ng-init.cc#L176)

### 原文（静的キー）

```text
%u overflow altars
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L240 — 未訳・API未接続

呼出: `mprf` / [原本 L240](../../crawl-ref/source/ng-init.cc#L240)

### 原文（静的キー）

```text
Malformed temple tag '%s' in map %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L253 — 未訳・API未接続

呼出: `mprf` / [原本 L253](../../crawl-ref/source/ng-init.cc#L253)

### 原文（静的キー）

```text
Malformed temple tag '%s' in map %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L263 — 未訳・API未接続

呼出: `mprf` / [原本 L263](../../crawl-ref/source/ng-init.cc#L263)

### 原文（静的キー）

```text
Map %s has temple_overflow_god tags but no count tag
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
