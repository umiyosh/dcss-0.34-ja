# コードメッセージ: `initfile.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L170 — 未訳・API未接続

呼出: `mprf` / [原本 L170](../../crawl-ref/source/initfile.cc#L170)

### 原文（静的キー）

```text
Options error: Bad monster_list_colour: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L178 — 未訳・API未接続

呼出: `mprf` / [原本 L178](../../crawl-ref/source/initfile.cc#L178)

### 原文（静的キー）

```text
Bad monster_list_colour key: %s

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L192 — 未訳・API未接続

呼出: `mprf` / [原本 L192](../../crawl-ref/source/initfile.cc#L192)

### 原文（静的キー）

```text
Options error: Bad custom_text_colours key: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L201 — 未訳・API未接続

呼出: `mprf` / [原本 L201](../../crawl-ref/source/initfile.cc#L201)

### 原文（静的キー）

```text
Bad custom_text_colours: %s

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L1307 — 未訳・API未接続

呼出: `mprf` / [原本 L1307](../../crawl-ref/source/initfile.cc#L1307)

### 原文（静的キー）

```text
Unknown species choice: %s

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L1342 — 未訳・API未接続

呼出: `mprf` / [原本 L1342](../../crawl-ref/source/initfile.cc#L1342)

### 原文（静的キー）

```text
Unknown background choice: %s

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L1740 — 未訳・API未接続

呼出: `mprf` / [原本 L1740](../../crawl-ref/source/initfile.cc#L1740)

### 原文（静的キー）

```text
Invalid glyph override: %X
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L2302 — 未訳・API未接続

呼出: `mprf` / [原本 L2302](../../crawl-ref/source/initfile.cc#L2302)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L2797 — 未訳・API未接続

呼出: `mprf` / [原本 L2797](../../crawl-ref/source/initfile.cc#L2797)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L2804 — 未訳・API未接続

呼出: `mprf` / [原本 L2804](../../crawl-ref/source/initfile.cc#L2804)

### 原文（静的キー）

```text
User lua is disabled in this build! `%s`
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L2820 — 未訳・API未接続

呼出: `mprf` / [原本 L2820](../../crawl-ref/source/initfile.cc#L2820)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L2827 — 未訳・API未接続

呼出: `mprf` / [原本 L2827](../../crawl-ref/source/initfile.cc#L2827)

### 原文（静的キー）

```text
User lua is disabled in this build! `%s`
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L2861 — 未訳・API未接続

呼出: `mprf` / [原本 L2861](../../crawl-ref/source/initfile.cc#L2861)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L2865 — 未訳・API未接続

呼出: `mprf` / [原本 L2865](../../crawl-ref/source/initfile.cc#L2865)

### 原文（静的キー）

```text
User lua is disabled in this build! (file: %s)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L3403 — 未訳・API未接続

呼出: `mprf` / [原本 L3403](../../crawl-ref/source/initfile.cc#L3403)

### 原文（静的キー）

```text
Options error: Missing colour in message_colour setting: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L3412 — 未訳・API未接続

呼出: `mprf` / [原本 L3412](../../crawl-ref/source/initfile.cc#L3412)

### 原文（静的キー）

```text
Options error: Bad colour '%s' in message_colour setting: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L3429 — 未訳・API未接続

呼出: `mprf` / [原本 L3429](../../crawl-ref/source/initfile.cc#L3429)

### 原文（静的キー）

```text
Options error: Missing tag/color in colour mapping: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L3448 — 未訳・API未接続

呼出: `mprf` / [原本 L3448](../../crawl-ref/source/initfile.cc#L3448)

### 原文（静的キー）

```text
Options error: Unknown color in colour mapping: '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L3470 — 未訳・API未接続

呼出: `mprf` / [原本 L3470](../../crawl-ref/source/initfile.cc#L3470)

### 原文（静的キー）

```text
Options error: invalid sound mapping '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L3481 — 未訳・API未接続

呼出: `mprf` / [原本 L3481](../../crawl-ref/source/initfile.cc#L3481)

### 原文（静的キー）

```text
Options warning: `sound` will have no effect on this build.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 21. L3495 — 未訳・API未接続

呼出: `mprf` / [原本 L3495](../../crawl-ref/source/initfile.cc#L3495)

### 原文（静的キー）

```text
Error parsing slot mapping: '%s'

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 22. L3507 — 未訳・API未接続

呼出: `mprf` / [原本 L3507](../../crawl-ref/source/initfile.cc#L3507)

### 原文（静的キー）

```text
Error parsing consumable mapping: '%s'

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 23. L3574 — 未訳・API未接続

呼出: `mprf` / [原本 L3574](../../crawl-ref/source/initfile.cc#L3574)

### 原文（静的キー）

```text
Setting crawl_dir to `%s`.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L3595 — 未訳・API未接続

呼出: `mprf` / [原本 L3595](../../crawl-ref/source/initfile.cc#L3595)

### 原文（静的キー）

```text
Bad bindkey bracketing in '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L3617 — 未訳・API未接続

呼出: `mprf` / [原本 L3617](../../crawl-ref/source/initfile.cc#L3617)

### 原文（静的キー）

```text
No key in bindkey directive '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L3629 — 未訳・API未接続

呼出: `mprf` / [原本 L3629](../../crawl-ref/source/initfile.cc#L3629)

### 原文（静的キー）

```text
Invalid keyseq '%s' in bindkey directive '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 27. L3642 — 未訳・API未接続

呼出: `mprf` / [原本 L3642](../../crawl-ref/source/initfile.cc#L3642)

### 原文（静的キー）

```text
Invalid key '%s' in bindkey directive '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 28. L3651 — 未訳・API未接続

呼出: `mprf` / [原本 L3651](../../crawl-ref/source/initfile.cc#L3651)

### 原文（静的キー）

```text
No command name for bindkey directive '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 29. L3660 — 未訳・API未接続

呼出: `mprf` / [原本 L3660](../../crawl-ref/source/initfile.cc#L3660)

### 原文（静的キー）

```text
No command named '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 30. L3871 — 未訳・API未接続

呼出: `mprf` / [原本 L3871](../../crawl-ref/source/initfile.cc#L3871)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 31. L3874 — 未訳・API未接続

呼出: `mprf` / [原本 L3874](../../crawl-ref/source/initfile.cc#L3874)

### 原文（静的キー）

```text
lua_file failed: clua not enabled on this build!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 32. L3945 — 未訳・API未接続

呼出: `mprf` / [原本 L3945](../../crawl-ref/source/initfile.cc#L3945)

### 原文（静的キー）

```text
Lua error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 33. L4673 — 未訳・API未接続

呼出: `mprf` / [原本 L4673](../../crawl-ref/source/initfile.cc#L4673)

### 原文（静的キー）

```text
Options error: %s (%s:%d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
