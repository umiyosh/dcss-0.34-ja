# コードメッセージ: `wiz-fsim.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L301 — 未訳・API未接続

呼出: `mprf` / [原本 L301](../../crawl-ref/source/wiz-fsim.cc#L301)

### 原文（静的キー）

```json
"Enter monster name (or MONS spec): "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L321 — 未訳・API未接続

呼出: `mpr` / [原本 L321](../../crawl-ref/source/wiz-fsim.cc#L321)

### 原文（静的キー）

```text
Failed to create monster.
```

### 日本語

未訳（英語にフォールバック）。

## 3. L339 — 未訳・API未接続

呼出: `mpr` / [原本 L339](../../crawl-ref/source/wiz-fsim.cc#L339)

### 原文（静的キー）

```text
Could not put monster adjacent to player.
```

### 日本語

未訳（英語にフォールバック）。

## 4. L531 — 未訳・API未接続

呼出: `mprf` / [原本 L531](../../crawl-ref/source/wiz-fsim.cc#L531)

### 原文（静的キー）

```text
Fighting %s with %s

```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L535 — 未訳・API未接続

呼出: `mprf` / [原本 L535](../../crawl-ref/source/wiz-fsim.cc#L535)

### 原文（静的キー）

```text
%8s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L536 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L536](../../crawl-ref/source/wiz-fsim.cc#L536)

### 未解決の式（断片を翻訳キーにしない）

```text
fdata.summary("Attack: ", false)
```

## 7. L539 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L539](../../crawl-ref/source/wiz-fsim.cc#L539)

### 未解決の式（断片を翻訳キーにしない）

```text
fdata.summary("Defend: ", false)
```

## 8. L622 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L622](../../crawl-ref/source/wiz-fsim.cc#L622)

### 未解決の式（断片を翻訳キーにしない）

```text
text_title
```

## 9. L645 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L645](../../crawl-ref/source/wiz-fsim.cc#L645)

### 未解決の式（断片を翻訳キーにしない）

```text
line
```

## 10. L652 — 未訳・API未接続

呼出: `mpr` / [原本 L652](../../crawl-ref/source/wiz-fsim.cc#L652)

### 原文（静的キー）

```text
Cancelling simulation.

```

### 日本語

未訳（英語にフォールバック）。

## 11. L670 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L670](../../crawl-ref/source/wiz-fsim.cc#L670)

### 未解決の式（断片を翻訳キーにしない）

```text
line
```

## 12. L709 — 未訳・API未接続

呼出: `mprf` / [原本 L709](../../crawl-ref/source/wiz-fsim.cc#L709)

### 原文（静的キー）

```text
%s %d, %s %d: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L717 — 未訳・API未接続

呼出: `mpr` / [原本 L717](../../crawl-ref/source/wiz-fsim.cc#L717)

### 原文（静的キー）

```text
Cancelling simulation.

```

### 日本語

未訳（英語にフォールバック）。

## 14. L739 — 未訳・API未接続

呼出: `mprf` / [原本 L739](../../crawl-ref/source/wiz-fsim.cc#L739)

### 原文（静的キー）

```text
Can't write %s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L753 — 未訳・API未接続

呼出: `mprf` / [原本 L753](../../crawl-ref/source/wiz-fsim.cc#L753)

### 原文（静的キー）

```text
(A)ttack or (D)efence?
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L807 — 未訳・API未接続

呼出: `mprf` / [原本 L807](../../crawl-ref/source/wiz-fsim.cc#L807)

### 原文（静的キー）

```text
Aborting sim on %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L809 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L809](../../crawl-ref/source/wiz-fsim.cc#L809)

### 未解決の式（断片を翻訳キーにしない）

```text
error
```

## 18. L824 — 未訳・API未接続

呼出: `mpr` / [原本 L824](../../crawl-ref/source/wiz-fsim.cc#L824)

### 原文（静的キー）

```text
Done.
```

### 日本語

未訳（英語にフォールバック）。
