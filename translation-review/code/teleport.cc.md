# コードメッセージ: `teleport.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L73 — 動的・未対応式（要調査・要改修）

呼出: `simple_monster_message` / [原本 L73](../../crawl-ref/source/teleport.cc#L73)

### 未解決の式（断片を翻訳キーにしない）

```text
message.c_str()
```

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 2. L220 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L220](../../crawl-ref/source/teleport.cc#L220)

### 原文（静的キー）

```text
 seems more stable.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 3. L225 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L225](../../crawl-ref/source/teleport.cc#L225)

### 原文（静的キー）

```text
 looks slightly unstable.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 4. L242 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L242](../../crawl-ref/source/teleport.cc#L242)

### 原文（静的キー）

```text
 flickers for a moment.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 5. L248 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L248](../../crawl-ref/source/teleport.cc#L248)

### 原文（静的キー）

```text
 disappears!
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 6. L258 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L258](../../crawl-ref/source/teleport.cc#L258)

### 原文（静的キー）

```text
 reappears nearby!
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 7. L260 — 未訳・API未接続

呼出: `mprf` / [原本 L260](../../crawl-ref/source/teleport.cc#L260)

### 原文（静的キー）

```text
%s appears out of thin air!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
