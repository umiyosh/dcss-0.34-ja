# コードメッセージ: `wiz-item.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L71 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L71](../../crawl-ref/source/wiz-item.cc#L71)

### 未解決の式（断片を翻訳キーにしない）

```text
book.name(DESC_PLAIN)
```

## 2. L78 — 未訳・API未接続

呼出: `mprf` / [原本 L78](../../crawl-ref/source/wiz-item.cc#L78)

### 原文（静的キー）

```json
"Enter name of item (or ITEM spec): "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L89 — 未訳・API未接続

呼出: `mprf` / [原本 L89](../../crawl-ref/source/wiz-item.cc#L89)

### 原文（静的キー）

```text
Error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L115 — 未訳・API未接続

呼出: `mpr` / [原本 L115](../../crawl-ref/source/wiz-item.cc#L115)

### 原文（静的キー）

```text
Could not allocate item.
```

### 日本語

未訳（英語にフォールバック）。

## 5. L148 — 未訳・API未接続

呼出: `mpr` / [原本 L148](../../crawl-ref/source/wiz-item.cc#L148)

### 原文（静的キー）

```text
No such monster.
```

### 日本語

未訳（英語にフォールバック）。

## 6. L154 — 未訳・API未接続

呼出: `mpr` / [原本 L154](../../crawl-ref/source/wiz-item.cc#L154)

### 原文（静的キー）

```text
That monster doesn't leave corpses.
```

### 日本語

未訳（英語にフォールバック）。

## 7. L160 — 未訳・API未接続

呼出: `mpr` / [原本 L160](../../crawl-ref/source/wiz-item.cc#L160)

### 原文（静的キー）

```text
You can't make a draconian corpse by its background.
```

### 日本語

未訳（英語にフォールバック）。

## 8. L176 — 未訳・API未接続

呼出: `mpr` / [原本 L176](../../crawl-ref/source/wiz-item.cc#L176)

### 原文（静的キー）

```text
Failed to create corpse.
```

### 日本語

未訳（英語にフォールバック）。

## 9. L208 — 未訳・API未接続

呼出: `mpr` / [原本 L208](../../crawl-ref/source/wiz-item.cc#L208)

### 原文（静的キー）

```text
No such item.
```

### 日本語

未訳（英語にフォールバック）。

## 10. L234 — 未訳・API未接続

呼出: `mprf` / [原本 L234](../../crawl-ref/source/wiz-item.cc#L234)

### 原文（静的キー）

```text
You can't tweak the randart properties of an equipped item.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L272 — 未訳・API未接続

呼出: `mprf` / [原本 L272](../../crawl-ref/source/wiz-item.cc#L272)

### 原文（静的キー）

```text
Toggling %s to %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L280 — 未訳・API未接続

呼出: `mprf` / [原本 L280](../../crawl-ref/source/wiz-item.cc#L280)

### 原文（静的キー）

```text
%s was %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 13. L285 — 未訳・API未接続

呼出: `mprf` / [原本 L285](../../crawl-ref/source/wiz-item.cc#L285)

### 原文（静的キー）

```text
Value for %s must be non-negative
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 14. L295 — 未訳・API未接続

呼出: `mprf` / [原本 L295](../../crawl-ref/source/wiz-item.cc#L295)

### 原文（静的キー）

```text
%s was %d.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 15. L304 — 未訳・API未接続

呼出: `mprf` / [原本 L304](../../crawl-ref/source/wiz-item.cc#L304)

### 原文（静的キー）

```text
%s was %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 16. L319 — 未訳・API未接続

呼出: `mprf` / [原本 L319](../../crawl-ref/source/wiz-item.cc#L319)

### 原文（静的キー）

```text
No such ego as: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 17. L324 — 未訳・API未接続

呼出: `mprf` / [原本 L324](../../crawl-ref/source/wiz-item.cc#L324)

### 原文（静的キー）

```text
Ego '%s' is invalid for %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 18. L387 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L387](../../crawl-ref/source/wiz-item.cc#L387)

### 未解決の式（断片を翻訳キーにしない）

```text
"Old value: %" PRId64" (0x%04" PRIx64")"
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 19. L389 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L389](../../crawl-ref/source/wiz-item.cc#L389)

### 未解決の式（断片を翻訳キーにしない）

```text
"Old value: 0x%08" PRIx64
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 20. L405 — 未訳・API未接続

呼出: `mpr` / [原本 L405](../../crawl-ref/source/wiz-item.cc#L405)

### 原文（静的キー）

```text
You can't set this flag on a non-artefact.
```

### 日本語

未訳（英語にフォールバック）。

## 21. L440 — 未訳・API未接続

呼出: `mprf` / [原本 L440](../../crawl-ref/source/wiz-item.cc#L440)

### 原文（静的キー）

```json
"Make book fixed [t]heme or fixed [l]evel? "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 22. L464 — 未訳・API未接続

呼出: `mprf` / [原本 L464](../../crawl-ref/source/wiz-item.cc#L464)

### 原文（静的キー）

```text
Real value: %d (known: %d)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 23. L466 — 未訳・API未接続

呼出: `mprf` / [原本 L466](../../crawl-ref/source/wiz-item.cc#L466)

### 原文（静的キー）

```text
Value: %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 24. L514 — 未訳・API未接続

呼出: `mprf` / [原本 L514](../../crawl-ref/source/wiz-item.cc#L514)

### 原文（静的キー）

```text
Failed to generate item for '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 25. L525 — 動的・未対応式（要調査・要改修）

呼出: `mprf` / [原本 L525](../../crawl-ref/source/wiz-item.cc#L525)

### 未解決の式（断片を翻訳キーにしない）

```text
item.base_type == OBJ_STAVES ? MSGCH_DIAGNOSTICS : MSGCH_ERROR
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 26. L573 — 未訳・API未接続

呼出: `mpr` / [原本 L573](../../crawl-ref/source/wiz-item.cc#L573)

### 原文（静的キー）

```text
That item is already an unrandom artefact.
```

### 日本語

未訳（英語にフォールバック）。

## 27. L579 — 未訳・API未接続

呼出: `mpr` / [原本 L579](../../crawl-ref/source/wiz-item.cc#L579)

### 原文（静的キー）

```text
That item cannot be turned into an artefact.
```

### 日本語

未訳（英語にフォールバック）。

## 28. L604 — 未訳・API未接続

呼出: `mprf` / [原本 L604](../../crawl-ref/source/wiz-item.cc#L604)

### 原文（静的キー）

```json
"Fake item as gift from which god (ENTER to leave alone): "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 29. L610 — 未訳・API未接続

呼出: `mpr` / [原本 L610](../../crawl-ref/source/wiz-item.cc#L610)

### 原文（静的キー）

```text
No such god, leaving item origin alone.
```

### 日本語

未訳（英語にフォールバック）。

## 30. L613 — 未訳・API未接続

呼出: `mprf` / [原本 L613](../../crawl-ref/source/wiz-item.cc#L613)

### 原文（静的キー）

```text
God gift of %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 31. L622 — 未訳・API未接続

呼出: `mpr` / [原本 L622](../../crawl-ref/source/wiz-item.cc#L622)

### 原文（静的キー）

```text
Failed to turn book into randart.
```

### 日本語

未訳（英語にフォールバック）。

## 32. L628 — 未訳・API未接続

呼出: `mpr` / [原本 L628](../../crawl-ref/source/wiz-item.cc#L628)

### 原文（静的キー）

```text
Failed to turn item into randart.
```

### 日本語

未訳（英語にフォールバック）。

## 33. L641 — 未訳・API未接続

呼出: `mpr` / [原本 L641](../../crawl-ref/source/wiz-item.cc#L641)

### 原文（静的キー）

```text
You feel a rush of knowledge.
```

### 日本語

未訳（英語にフォールバック）。

## 34. L658 — 未訳・API未接続

呼出: `mpr` / [原本 L658](../../crawl-ref/source/wiz-item.cc#L658)

### 原文（静的キー）

```text
You feel a rush of antiknowledge.
```

### 日本語

未訳（英語にフォールバック）。

## 35. L676 — 未訳・API未接続

呼出: `mpr` / [原本 L676](../../crawl-ref/source/wiz-item.cc#L676)

### 原文（静的キー）

```text
Item stacks (by location and top item):
```

### 日本語

未訳（英語にフォールバック）。

## 36. L681 — 未訳・API未接続

呼出: `mprf` / [原本 L681](../../crawl-ref/source/wiz-item.cc#L681)

### 原文（静的キー）

```text
(%2d,%2d): %s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 37. L687 — 未訳・API未接続

呼出: `mpr` / [原本 L687](../../crawl-ref/source/wiz-item.cc#L687)

### 原文（静的キー）

```text

```

### 日本語

未訳（英語にフォールバック）。

## 38. L688 — 未訳・API未接続

呼出: `mpr` / [原本 L688](../../crawl-ref/source/wiz-item.cc#L688)

### 原文（静的キー）

```text
Floor items (stacks only show top item):
```

### 日本語

未訳（英語にフォールバック）。

## 39. L696 — 未訳・API未接続

呼出: `mprf` / [原本 L696](../../crawl-ref/source/wiz-item.cc#L696)

### 原文（静的キー）

```text
%3d at (%2d,%2d): %s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 40. L759 — 未訳・API未接続

呼出: `mpr` / [原本 L759](../../crawl-ref/source/wiz-item.cc#L759)

### 原文（静的キー）

```text
Too many items on level.
```

### 日本語

未訳（英語にフォールバック）。

## 41. L791 — 未訳・API未接続

呼出: `mprf` / [原本 L791](../../crawl-ref/source/wiz-item.cc#L791)

### 原文（静的キー）

```text
Can't write to acquirement_stats.txt: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 42. L814 — 未訳・API未接続

呼出: `mpr` / [原本 L814](../../crawl-ref/source/wiz-item.cc#L814)

### 原文（静的キー）

```text
Stopping early due to keyboard input.
```

### 日本語

未訳（英語にフォールバック）。

## 43. L823 — 未訳・API未接続

呼出: `mpr` / [原本 L823](../../crawl-ref/source/wiz-item.cc#L823)

### 原文（静的キー）

```text
Acquirement failed, stopping early.
```

### 日本語

未訳（英語にフォールバック）。

## 44. L876 — 未訳・API未接続

呼出: `mprf` / [原本 L876](../../crawl-ref/source/wiz-item.cc#L876)

### 原文（静的キー）

```text
%d%% done.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 45. L884 — 未訳・API未接続

呼出: `mpr` / [原本 L884](../../crawl-ref/source/wiz-item.cc#L884)

### 原文（静的キー）

```text
No items generated.
```

### 日本語

未訳（英語にフォールバック）。

## 46. L1110 — 未訳・API未接続

呼出: `mpr` / [原本 L1110](../../crawl-ref/source/wiz-item.cc#L1110)

### 原文（静的キー）

```text
Results written into acquirement_stats.txt.
```

### 日本語

未訳（英語にフォールバック）。

## 47. L1125 — 未訳・API未接続

呼出: `mprf` / [原本 L1125](../../crawl-ref/source/wiz-item.cc#L1125)

### 原文（静的キー）

```json
"Enter name of item (or ITEM spec): "
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 48. L1136 — 未訳・API未接続

呼出: `mprf` / [原本 L1136](../../crawl-ref/source/wiz-item.cc#L1136)

### 原文（静的キー）

```text
Error: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 49. L1174 — 未訳・API未接続

呼出: `mprf` / [原本 L1174](../../crawl-ref/source/wiz-item.cc#L1174)

### 原文（静的キー）

```text
Can't write to randart_stats.txt: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 50. L1185 — 未訳・API未接続

呼出: `mpr` / [原本 L1185](../../crawl-ref/source/wiz-item.cc#L1185)

### 原文（静的キー）

```text
Stopping early due to keyboard input.
```

### 日本語

未訳（英語にフォールバック）。

## 51. L1205 — 未訳・API未接続

呼出: `mprf` / [原本 L1205](../../crawl-ref/source/wiz-item.cc#L1205)

### 原文（静的キー）

```text
Unable to make artefact from '%s'
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 52. L1268 — 未訳・API未接続

呼出: `mprf` / [原本 L1268](../../crawl-ref/source/wiz-item.cc#L1268)

### 原文（静的キー）

```text
%d%% done.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 53. L1330 — 未訳・API未接続

呼出: `mpr` / [原本 L1330](../../crawl-ref/source/wiz-item.cc#L1330)

### 原文（静的キー）

```text
Results written into 'randart_stats.txt'.
```

### 日本語

未訳（英語にフォールバック）。

## 54. L1336 — 未訳・API未接続

呼出: `mpr` / [原本 L1336](../../crawl-ref/source/wiz-item.cc#L1336)

### 原文（静的キー）

```text
Generate stats for: [a] acquirement [b] randart properties
```

### 日本語

未訳（英語にフォールバック）。

## 55. L1356 — 未訳・API未接続

呼出: `mpr` / [原本 L1356](../../crawl-ref/source/wiz-item.cc#L1356)

### 原文（静的キー）

```text
Unknown card.
```

### 日本語

未訳（英語にフォールバック）。

## 56. L1377 — 未訳・API未接続

呼出: `mpr` / [原本 L1377](../../crawl-ref/source/wiz-item.cc#L1377)

### 原文（静的キー）

```text
Unknown card.
```

### 日本語

未訳（英語にフォールバック）。

## 57. L1431 — 未訳・API未接続

呼出: `mpr` / [原本 L1431](../../crawl-ref/source/wiz-item.cc#L1431)

### 原文（静的キー）

```text
Evokers recharged.
```

### 日本語

未訳（英語にフォールバック）。

## 58. L1441 — 未訳・API未接続

呼出: `mpr` / [原本 L1441](../../crawl-ref/source/wiz-item.cc#L1441)

### 原文（静的キー）

```text
Unobtained all runes and the Orb of Zot.
```

### 日本語

未訳（英語にフォールバック）。
