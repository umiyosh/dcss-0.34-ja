# コードメッセージ: `ranged-attack.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L188 — 未訳・API未接続

呼出: `mprf` / [原本 L188](../../crawl-ref/source/ranged-attack.cc#L188)

### 原文（静的キー）

```text
%s %s the %s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L211 — 未訳・API未接続

呼出: `mprf` / [原本 L211](../../crawl-ref/source/ranged-attack.cc#L211)

### 原文（静的キー）

```text
The %s is repelled.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L224 — 未訳・API未接続

呼出: `mprf` / [原本 L224](../../crawl-ref/source/ranged-attack.cc#L224)

### 原文（静的キー）

```text
The %s%s misses %s.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L246 — 未訳・API未接続

呼出: `mprf` / [原本 L246](../../crawl-ref/source/ranged-attack.cc#L246)

### 原文（静的キー）

```text
Your attached jelly eats the %s!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L302 — 未訳・API未接続

呼出: `mprf` / [原本 L302](../../crawl-ref/source/ranged-attack.cc#L302)

### 原文（静的キー）

```text
The %s %s %s%s but does no damage.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L529 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L529](../../crawl-ref/source/ranged-attack.cc#L529)

### 原文（静的キー）

```text
 is unaffected.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 7. L567 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L567](../../crawl-ref/source/ranged-attack.cc#L567)

### 原文（静的キー）

```text
 resists.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 8. L694 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L694](../../crawl-ref/source/ranged-attack.cc#L694)

### 原文（静的キー）

```text
 is unaffected.
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 9. L700 — 未訳・API未接続

呼出: `mprf` / [原本 L700](../../crawl-ref/source/ranged-attack.cc#L700)

### 原文（静的キー）

```text
You become untethered in space!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L710 — 未訳・API未接続

呼出: `simple_monster_message` / [原本 L710](../../crawl-ref/source/ranged-attack.cc#L710)

### 原文（静的キー）

```text
 becomes untethered in space!
```

### 日本語

未訳（英語にフォールバック）。

主語等を別途結合する部分文。日本語の語順・文法を含む表示経路の改修が必要。

## 11. L760 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L760](../../crawl-ref/source/ranged-attack.cc#L760)

### 未解決の式（断片を翻訳キーにしない）

```text
special_damage_message
```

## 12. L802 — 未訳・API未接続

呼出: `mprf` / [原本 L802](../../crawl-ref/source/ranged-attack.cc#L802)

### 原文（静的キー）

```text
The %s %s %s%s%s%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
