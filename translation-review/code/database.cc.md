# コードメッセージ: `database.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L298 — 未訳・API未接続

呼出: `mprf` / [原本 L298](../../crawl-ref/source/database.cc#L298)

### 原文（静的キー）

```text
Regenerating db: %s [%s]
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L305 — 未訳・API未接続

呼出: `mprf` / [原本 L305](../../crawl-ref/source/database.cc#L305)

### 原文（静的キー）

```text
Regenerating db: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L453 — 未訳・API未接続

呼出: `mprf` / [原本 L453](../../crawl-ref/source/database.cc#L453)

### 原文（静的キー）

```text
Unbalanced {{, bailing.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L488 — 未訳・API未接続

呼出: `mprf` / [原本 L488](../../crawl-ref/source/database.cc#L488)

### 原文（静的キー）

```text
Unbalanced [[, bailing.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L694 — 未訳・API未接続

呼出: `mprf` / [原本 L694](../../crawl-ref/source/database.cc#L694)

### 原文（静的キー）

```text
Too many nested replacements, bailing.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L720 — 未訳・API未接続

呼出: `mprf` / [原本 L720](../../crawl-ref/source/database.cc#L720)

### 原文（静的キー）

```text
Too many string replacements, bailing.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L727 — 未訳・API未接続

呼出: `mprf` / [原本 L727](../../crawl-ref/source/database.cc#L727)

### 原文（静的キー）

```text
Unbalanced @, bailing.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L998 — 動的・未対応式（要調査・要改修）

呼出: `jtrans_format` / [原本 L998](../../crawl-ref/source/database.cc#L998)

### 未解決の式（断片を翻訳キーにしない）

```text
format
```

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
