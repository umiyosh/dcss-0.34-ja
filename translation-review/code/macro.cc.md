# コードメッセージ: `macro.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L791 — 未訳・API未接続

呼出: `mprf` / [原本 L791](../../crawl-ref/source/macro.cc#L791)

### 原文（静的キー）

```text
Couldn't open %s for writing!
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L829 — 未訳・API未接続

呼出: `mprf` / [原本 L829](../../crawl-ref/source/macro.cc#L829)

### 原文（静的キー）

```text
(Key replay ran out of keys)
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L960 — 未訳・API未接続

呼出: `mprf` / [原本 L960](../../crawl-ref/source/macro.cc#L960)

### 原文（静的キー）

```text
Flushing non-empty key buffer (Buffer is '%s')
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L966 — 未訳・API未接続

呼出: `mprf` / [原本 L966](../../crawl-ref/source/macro.cc#L966)

### 原文（静的キー）

```text
Flushing non-empty key buffer
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L1666 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L1666](../../crawl-ref/source/macro.cc#L1666)

### 未解決の式（断片を翻訳キーにしない）

```text
menu.status_msg
```

## 6. L2179 — 未訳・API未接続

呼出: `mprf` / [原本 L2179](../../crawl-ref/source/macro.cc#L2179)

### 原文（静的キー）

```text
key_to_command(): command '%s' (%d:%d) wrong for desired context %d
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L2288 — 未訳・API未接続

呼出: `mprf` / [原本 L2288](../../crawl-ref/source/macro.cc#L2288)

### 原文（静的キー）

```text
Cannot bind command #%d to a key.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 8. L2293 — 未訳・API未接続

呼出: `mprf` / [原本 L2293](../../crawl-ref/source/macro.cc#L2293)

### 原文（静的キー）

```text
Cannot bind command '%s' to a key.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 9. L2303 — 未訳・API未接続

呼出: `mprf` / [原本 L2303](../../crawl-ref/source/macro.cc#L2303)

### 原文（静的キー）

```text
Ignoring menu bindkey of %s for webtiles client
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 10. L2311 — 未訳・API未接続

呼出: `mprf` / [原本 L2311](../../crawl-ref/source/macro.cc#L2311)

### 原文（静的キー）

```text
Cannot bind user function keys to a command.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 11. L2317 — 未訳・API未接続

呼出: `mprf` / [原本 L2317](../../crawl-ref/source/macro.cc#L2317)

### 原文（静的キー）

```text
Cannot bind synthetic keys to a command.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 12. L2323 — 未訳・API未接続

呼出: `mprf` / [原本 L2323](../../crawl-ref/source/macro.cc#L2323)

### 原文（静的キー）

```text
Key %d in context %d cannot be rebound.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
