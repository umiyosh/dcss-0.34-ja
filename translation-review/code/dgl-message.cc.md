# コードメッセージ: `dgl-message.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L30 — 動的・未対応式（要調査・要改修）

呼出: `mpr` / [原本 L30](../../crawl-ref/source/dgl-message.cc#L30)

### 未解決の式（断片を翻訳キーにしない）

```text
line
```

## 2. L60 — 未訳・API未接続

呼出: `mprf` / [原本 L60](../../crawl-ref/source/dgl-message.cc#L60)

### 原文（静的キー）

```text
Couldn't read %s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 3. L71 — 未訳・API未接続

呼出: `mprf` / [原本 L71](../../crawl-ref/source/dgl-message.cc#L71)

### 原文（静的キー）

```text
Failed to lock %s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 4. L89 — 未訳・API未接続

呼出: `mprf` / [原本 L89](../../crawl-ref/source/dgl-message.cc#L89)

### 原文（静的キー）

```text
Your messages:
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 5. L98 — 未訳・API未接続

呼出: `mprf` / [原本 L98](../../crawl-ref/source/dgl-message.cc#L98)

### 原文（静的キー）

```text
Failed to lock %s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 6. L107 — 未訳・API未接続

呼出: `mprf` / [原本 L107](../../crawl-ref/source/dgl-message.cc#L107)

### 原文（静的キー）

```text
Unable to write lock %s: %s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 7. L128 — 未訳・API未接続

呼出: `mprf` / [原本 L128](../../crawl-ref/source/dgl-message.cc#L128)

### 原文（静的キー）

```text
Beep! Your pager goes off! Use _ to check your messages.
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。
