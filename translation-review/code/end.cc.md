# コードメッセージ: `end.cc`

[コードメッセージ一覧](README.md)

自動生成。原本・辞書を変更して共通生成コマンドで更新する。
API接続済みは採用済み・実表示確認済みを意味しない。

## 1. L125 — 未訳・API未接続

呼出: `mprf` / [原本 L125](../../crawl-ref/source/end.cc#L125)

### 原文（静的キー）

```text
%s
```

### 日本語

未訳（英語にフォールバック）。

書式と引数の対応を確認する。引数に含まれる英語の名称・句は別途対応が必要。

## 2. L315 — 未訳・API未接続

呼出: `mpr` / [原本 L315](../../crawl-ref/source/end.cc#L315)

### 原文（静的キー）

```text
Char dump unsuccessful! Sorry about that.
```

### 日本語

未訳（英語にフォールバック）。
