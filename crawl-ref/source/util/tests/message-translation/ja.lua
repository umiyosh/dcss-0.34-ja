test.eq(crawl.jtrans("You blink."), "試験用：瞬間移動した。")
test.eq(crawl.jtrans("Case Key"), "大文字のキー")
test.eq(crawl.jtrans("case key"), "小文字のキー")
test.eq(crawl.jtrans("CASE KEY"), "CASE KEY")
test.eq(crawl.jtrans(" Exact key with spaces. "), "空白付きのキー")
test.eq(crawl.jtrans("Exact key with spaces."), "Exact key with spaces.")
test.eq(crawl.jtrans("line one\nline two"), "一行目\n二行目")
test.eq(crawl.jtrans("Untranslated fixture."), "Untranslated fixture.")
test.eq(crawl.jtrans("one\0two"), "one\0two")
test.eq(crawl.jtrans("#literal"), "コメントではない")
test.eq(crawl.jtrans("%%%%literal"), "区切りではない")
test.eq(crawl.jtrans("TIMESTAMP"), "メタデータではない")
test.eq(crawl.jtrans("Literal alias"), "<case key>")
test.eq(crawl.jtrans("Literal Lua"), '{{error("must not execute")}}')
test.eq(string.format(crawl.jtrans_format("Fixture %s: %d%%"), "x", 12),
        "試験x：12%")
test.eq(string.format(crawl.jtrans_format("Unsafe %s"), "x"), "Unsafe x")
crawl.clear_message_store()
crawl.mpr(crawl.jtrans("You blink."))
crawl.flush_prev_message()
assert(string.find(crawl.messages(1), "試験用：瞬間移動した。", 1, true))
crawl.stderr("message-translation fixture passed: ja")
